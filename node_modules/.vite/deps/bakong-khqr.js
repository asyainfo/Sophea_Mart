import { t as __commonJSMin } from "./chunk-FDOR9p9I.js";
import { t as require_buffer } from "./buffer-CIOeS1Lc.js";
//#region node_modules/bakong-khqr/src/constant/khqrData.js
var require_khqrData = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = {
		currency: {
			usd: 840,
			khr: 116
		},
		merchantType: {
			merchant: "merchant",
			individual: "individual"
		}
	};
}));
//#endregion
//#region node_modules/bakong-khqr/src/constant/errorCode.js
var require_errorCode = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = {
		BAKONG_ACCOUNT_ID_REQUIRED: {
			code: 1,
			message: "Bakong Account ID cannot be null or empty"
		},
		MERCHANT_NAME_REQUIRED: {
			code: 2,
			message: "Merchant name cannot be null or empty"
		},
		BAKONG_ACCOUNT_ID_INVALID: {
			code: 3,
			message: "Bakong Account ID is invalid"
		},
		TRANSACTION_AMOUNT_INVALID: {
			code: 4,
			message: "Amount is invalid"
		},
		MERCHANT_TYPE_REQUIRED: {
			code: 5,
			message: "Merchant type cannot be null or empty"
		},
		BAKONG_ACCOUNT_ID_LENGTH_INVALID: {
			code: 6,
			message: "Bakong Account ID Length is Invalid"
		},
		MERCHANT_NAME_LENGTH_INVALID: {
			code: 7,
			message: "Merchant Name Length is invalid"
		},
		KHQR_INVALID: {
			code: 8,
			message: "KHQR provided is invalid"
		},
		CURRENCY_TYPE_REQUIRED: {
			code: 9,
			message: "Currency type cannot be null or empty"
		},
		BILL_NUMBER_LENGTH_INVALID: {
			code: 10,
			message: "Bill Name Length is invalid"
		},
		STORE_LABEL_LENGTH_INVALID: {
			code: 11,
			message: "Store Label Length is invalid"
		},
		TERMINAL_LABEL_LENGTH_INVALID: {
			code: 12,
			message: "Terminal Label Length is invalid"
		},
		CONNECTION_TIMEOUT: {
			code: 13,
			message: "Cannot reach Bakong Open API service. Please check internet connection"
		},
		INVALID_DEEP_LINK_SOURCE_INFO: {
			code: 14,
			message: "Source Info for Deep Link is invalid"
		},
		INTERNAL_SREVER: {
			code: 15,
			message: "Internal server error"
		},
		PAYLOAD_FORMAT_INDICATOR_LENGTH_INVALID: {
			code: 16,
			message: "Payload Format indicator Length is invalid"
		},
		POINT_INITIATION_LENGTH_INVALID: {
			code: 17,
			message: "Point of initiation Length is invalid"
		},
		MERCHANT_CODE_LENGTH_INVALID: {
			code: 18,
			message: "Merchant code Length is invalid"
		},
		TRANSACTION_CURRENCY_LENGTH_INVALID: {
			code: 19,
			message: "Transaction currency Length is invalid"
		},
		COUNTRY_CODE_LENGTH_INVALID: {
			code: 20,
			message: "Country code Length is invalid"
		},
		MERCHANT_CITY_LENGTH_INVALID: {
			code: 21,
			message: "Merchant city Length is invalid"
		},
		CRC_LENGTH_INVALID: {
			code: 22,
			message: "CRC Length is invalid"
		},
		PAYLOAD_FORMAT_INDICATOR_TAG_REQUIRED: {
			code: 23,
			message: "Payload format indicator tag required"
		},
		CRC_TAG_REQUIRED: {
			code: 24,
			message: "CRC tag required"
		},
		MERCHANT_CATEGORY_TAG_REQUIRED: {
			code: 25,
			message: "Merchant category tag required"
		},
		COUNTRY_CODE_TAG_REQUIRED: {
			code: 26,
			message: "Country Code cannot be null or empty"
		},
		MERCHANT_CITY_TAG_REQUIRED: {
			code: 27,
			message: "Merchant City cannot be null or empty"
		},
		UNSUPPORTED_CURRENCY: {
			code: 28,
			message: "Unsupported currency"
		},
		INVALID_DEEP_LINK_URL: {
			code: 29,
			message: "Deep Link URL is not valid"
		},
		MERCHANT_ID_REQUIRED: {
			code: 30,
			message: "Merchant ID cannot be null or empty"
		},
		ACQUIRING_BANK_REQUIRED: {
			code: 31,
			message: "Acquiring Bank cannot be null or empty"
		},
		MERCHANT_ID_LENGTH_INVALID: {
			code: 32,
			message: "Merchant ID Length is invalid"
		},
		ACQUIRING_BANK_LENGTH_INVALID: {
			code: 33,
			message: "Acquiring Bank Length is invalid"
		},
		MOBILE_NUMBER_LENGTH_INVALID: {
			code: 34,
			message: "Mobile Number Length is invalid"
		},
		ACCOUNT_INFORMATION_LENGTH_INVALID: {
			code: 35,
			message: "Account Information Length is invalid"
		},
		TAG_NOT_IN_ORDER: {
			code: 36,
			message: "Tag is not in order"
		},
		LANGUAGE_PREFERENCE_REQUIRED: {
			code: 37,
			message: "Language Preference cannot be null or empty"
		},
		LANGUAGE_PREFERENCE_LENGTH_INVALID: {
			code: 38,
			message: "Language Preference Length is invalid"
		},
		MERCHANT_NAME_ALTERNATE_LANGUAGE_REQUIRED: {
			code: 39,
			message: "Merchant Name Alternate Language cannot be null or empty"
		},
		MERCHANT_NAME_ALTERNATE_LANGUAGE_LENGTH_INVALID: {
			code: 40,
			message: "Merchant Name Alternate Language Length is invalid"
		},
		MERCHANT_CITY_ALTERNATE_LANGUAGE_LENGTH_INVALID: {
			code: 41,
			message: "Merchant City Alternate Language Length is invalid"
		},
		PURPOSE_OF_TRANSACTION_LENGTH_INVALID: {
			code: 42,
			message: "Purpose of Transaction Length is invalid"
		},
		UPI_ACCOUNT_INFORMATION_LENGTH_INVALID: {
			code: 43,
			message: "Upi Account Information Length is invalid"
		},
		UPI_ACCOUNT_INFORMATION_INVALID_CURRENCY: {
			code: 44,
			message: "Upi Account Information Length does not accept USD"
		},
		EXPIRATION_TIMESTAMP_REQUIRED: {
			code: 45,
			message: "Expiration timestamp is required for dynamic KHQR"
		},
		KHQR_EXPIRED: {
			code: 46,
			message: "This dynamic KHQR has expired"
		},
		INVALID_DYNAMIC_KHQR: {
			code: 47,
			message: "This dynamic KHQR has invalid field transaction amount"
		},
		POINT_OF_INITIATION_METHOD_INVALID: {
			code: 48,
			message: "Point of Initiation Method is invalid"
		},
		EXPIRATION_TIMESTAMP_LENGTH_INVALID: {
			code: 49,
			message: "Expiration timestamp length is invalid"
		},
		EXPIRATION_TIMESTAMP_IN_THE_PAST: {
			code: 50,
			message: "Expiration timestamp is in the past"
		},
		INVALID_MERCHANT_CATEGORY_CODE: {
			code: 51,
			message: "Invalid merchant category code"
		}
	};
}));
//#endregion
//#region node_modules/bakong-khqr/src/constant/index.js
var require_constant = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var khqrData = require_khqrData();
	module.exports = {
		emv: {
			PAYLOAD_FORMAT_INDICATOR: "00",
			DEFAULT_PAYLOAD_FORMAT_INDICATOR: "01",
			POINT_OF_INITIATION_METHOD: "01",
			STATIC_QR: "11",
			DYNAMIC_QR: "12",
			MERCHANT_ACCOUNT_INFORMATION_INDIVIDUAL: "29",
			MERCHANT_ACCOUNT_INFORMATION_MERCHANT: "30",
			BAKONG_ACCOUNT_IDENTIFIER: "00",
			MERCHANT_ACCOUNT_INFORMATION_MERCHANT_ID: "01",
			INDIVIDUAL_ACCOUNT_INFORMATION: "01",
			MERCHANT_ACCOUNT_INFORMATION_ACQUIRING_BANK: "02",
			MERCHANT_CATEGORY_CODE: "52",
			DEFAULT_MERCHANT_CATEGORY_CODE: "5999",
			TRANSACTION_CURRENCY: "53",
			TRANSACTION_AMOUNT: "54",
			DEFAULT_TRANSACTION_AMOUNT: "0",
			COUNTRY_CODE: "58",
			DEFAULT_COUNTRY_CODE: "KH",
			MERCHANT_NAME: "59",
			MERCHANT_CITY: "60",
			DEFAULT_MERCHANT_CITY: "Phnom Penh",
			CRC: "63",
			CRC_LENGTH: "04",
			ADDITIONAL_DATA_TAG: "62",
			BILLNUMBER_TAG: "01",
			ADDITIONAL_DATA_FIELD_MOBILE_NUMBER: "02",
			STORELABEL_TAG: "03",
			TERMINAL_TAG: "07",
			PURPOSE_OF_TRANSACTION: "08",
			TIMESTAMP_TAG: "99",
			CREATION_TIMESTAMP: "00",
			EXPIRATION_TIMESTAMP: "01",
			MERCHANT_INFORMATION_LANGUAGE_TEMPLATE: "64",
			LANGUAGE_PREFERENCE: "00",
			MERCHANT_NAME_ALTERNATE_LANGUAGE: "01",
			MERCHANT_CITY_ALTERNATE_LANGUAGE: "02",
			UNIONPAY_MERCHANT_ACCOUNT: "15",
			INVALID_LENGTH: {
				KHQR: 12,
				MERCHANT_NAME: 25,
				BAKONG_ACCOUNT: 32,
				AMOUNT: 13,
				COUNTRY_CODE: 3,
				MERCHANT_CATEGORY_CODE: 4,
				MERCHANT_CITY: 15,
				TIMESTAMP: 13,
				TRANSACTION_AMOUNT: 14,
				TRANSACTION_CURRENCY: 3,
				BILL_NUMBER: 25,
				STORE_LABEL: 25,
				TERMINAL_LABEL: 25,
				PURPOSE_OF_TRANSACTION: 25,
				MERCHANT_ID: 32,
				ACQUIRING_BANK: 32,
				MOBILE_NUMBER: 25,
				ACCOUNT_INFORMATION: 32,
				MERCHANT_INFORMATION_LANGUAGE_TEMPLATE: 99,
				UPI_MERCHANT: 99,
				LANGUAGE_PREFERENCE: 2,
				MERCHANT_NAME_ALTERNATE_LANGUAGE: 25,
				MERCHANT_CITY_ALTERNATE_LANGUAGE: 15
			}
		},
		errorCode: require_errorCode(),
		khqrData
	};
}));
//#endregion
//#region node_modules/bakong-khqr/src/helper/crc16.js
var require_crc16 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var Buffer = require_buffer().Buffer;
	var crcTable = [
		0,
		4129,
		8258,
		12387,
		16516,
		20645,
		24774,
		28903,
		33032,
		37161,
		41290,
		45419,
		49548,
		53677,
		57806,
		61935,
		4657,
		528,
		12915,
		8786,
		21173,
		17044,
		29431,
		25302,
		37689,
		33560,
		45947,
		41818,
		54205,
		50076,
		62463,
		58334,
		9314,
		13379,
		1056,
		5121,
		25830,
		29895,
		17572,
		21637,
		42346,
		46411,
		34088,
		38153,
		58862,
		62927,
		50604,
		54669,
		13907,
		9842,
		5649,
		1584,
		30423,
		26358,
		22165,
		18100,
		46939,
		42874,
		38681,
		34616,
		63455,
		59390,
		55197,
		51132,
		18628,
		22757,
		26758,
		30887,
		2112,
		6241,
		10242,
		14371,
		51660,
		55789,
		59790,
		63919,
		35144,
		39273,
		43274,
		47403,
		23285,
		19156,
		31415,
		27286,
		6769,
		2640,
		14899,
		10770,
		56317,
		52188,
		64447,
		60318,
		39801,
		35672,
		47931,
		43802,
		27814,
		31879,
		19684,
		23749,
		11298,
		15363,
		3168,
		7233,
		60846,
		64911,
		52716,
		56781,
		44330,
		48395,
		36200,
		40265,
		32407,
		28342,
		24277,
		20212,
		15891,
		11826,
		7761,
		3696,
		65439,
		61374,
		57309,
		53244,
		48923,
		44858,
		40793,
		36728,
		37256,
		33193,
		45514,
		41451,
		53516,
		49453,
		61774,
		57711,
		4224,
		161,
		12482,
		8419,
		20484,
		16421,
		28742,
		24679,
		33721,
		37784,
		41979,
		46042,
		49981,
		54044,
		58239,
		62302,
		689,
		4752,
		8947,
		13010,
		16949,
		21012,
		25207,
		29270,
		46570,
		42443,
		38312,
		34185,
		62830,
		58703,
		54572,
		50445,
		13538,
		9411,
		5280,
		1153,
		29798,
		25671,
		21540,
		17413,
		42971,
		47098,
		34713,
		38840,
		59231,
		63358,
		50973,
		55100,
		9939,
		14066,
		1681,
		5808,
		26199,
		30326,
		17941,
		22068,
		55628,
		51565,
		63758,
		59695,
		39368,
		35305,
		47498,
		43435,
		22596,
		18533,
		30726,
		26663,
		6336,
		2273,
		14466,
		10403,
		52093,
		56156,
		60223,
		64286,
		35833,
		39896,
		43963,
		48026,
		19061,
		23124,
		27191,
		31254,
		2801,
		6864,
		10931,
		14994,
		64814,
		60687,
		56684,
		52557,
		48554,
		44427,
		40424,
		36297,
		31782,
		27655,
		23652,
		19525,
		15522,
		11395,
		7392,
		3265,
		61215,
		65342,
		53085,
		57212,
		44955,
		49082,
		36825,
		40952,
		28183,
		32310,
		20053,
		24180,
		11923,
		16050,
		3793,
		7920
	];
	function crc16(s) {
		var crc = 65535;
		var j, i;
		s = Buffer.from(s);
		for (i = 0; i < s.length; i++) {
			c = s[i];
			j = (c ^ crc >> 8) & 255;
			crc = crcTable[j] ^ crc << 8;
		}
		return getHexString((crc ^ 0) & 65535);
	}
	function getHexString(crc) {
		return pad(crc.toString(16).toUpperCase(), 4);
	}
	function pad(num, size) {
		num = num.toString();
		while (num.length < size) num = "0" + num;
		return num;
	}
	module.exports = crc16;
}));
//#endregion
//#region node_modules/bakong-khqr/src/CRCValidation.js
var require_CRCValidation = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var CRCValidation = class {
		constructor(valid) {
			this.isValid = valid;
		}
	};
	module.exports = CRCValidation;
}));
//#endregion
//#region node_modules/axios/lib/helpers/bind.js
var require_bind = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = function bind(fn, thisArg) {
		return function wrap() {
			var args = new Array(arguments.length);
			for (var i = 0; i < args.length; i++) args[i] = arguments[i];
			return fn.apply(thisArg, args);
		};
	};
}));
//#endregion
//#region node_modules/axios/lib/utils.js
var require_utils = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var bind = require_bind();
	var toString = Object.prototype.toString;
	/**
	* Determine if a value is an Array
	*
	* @param {Object} val The value to test
	* @returns {boolean} True if value is an Array, otherwise false
	*/
	function isArray(val) {
		return toString.call(val) === "[object Array]";
	}
	/**
	* Determine if a value is undefined
	*
	* @param {Object} val The value to test
	* @returns {boolean} True if the value is undefined, otherwise false
	*/
	function isUndefined(val) {
		return typeof val === "undefined";
	}
	/**
	* Determine if a value is a Buffer
	*
	* @param {Object} val The value to test
	* @returns {boolean} True if value is a Buffer, otherwise false
	*/
	function isBuffer(val) {
		return val !== null && !isUndefined(val) && val.constructor !== null && !isUndefined(val.constructor) && typeof val.constructor.isBuffer === "function" && val.constructor.isBuffer(val);
	}
	/**
	* Determine if a value is an ArrayBuffer
	*
	* @param {Object} val The value to test
	* @returns {boolean} True if value is an ArrayBuffer, otherwise false
	*/
	function isArrayBuffer(val) {
		return toString.call(val) === "[object ArrayBuffer]";
	}
	/**
	* Determine if a value is a FormData
	*
	* @param {Object} val The value to test
	* @returns {boolean} True if value is an FormData, otherwise false
	*/
	function isFormData(val) {
		return typeof FormData !== "undefined" && val instanceof FormData;
	}
	/**
	* Determine if a value is a view on an ArrayBuffer
	*
	* @param {Object} val The value to test
	* @returns {boolean} True if value is a view on an ArrayBuffer, otherwise false
	*/
	function isArrayBufferView(val) {
		var result;
		if (typeof ArrayBuffer !== "undefined" && ArrayBuffer.isView) result = ArrayBuffer.isView(val);
		else result = val && val.buffer && val.buffer instanceof ArrayBuffer;
		return result;
	}
	/**
	* Determine if a value is a String
	*
	* @param {Object} val The value to test
	* @returns {boolean} True if value is a String, otherwise false
	*/
	function isString(val) {
		return typeof val === "string";
	}
	/**
	* Determine if a value is a Number
	*
	* @param {Object} val The value to test
	* @returns {boolean} True if value is a Number, otherwise false
	*/
	function isNumber(val) {
		return typeof val === "number";
	}
	/**
	* Determine if a value is an Object
	*
	* @param {Object} val The value to test
	* @returns {boolean} True if value is an Object, otherwise false
	*/
	function isObject(val) {
		return val !== null && typeof val === "object";
	}
	/**
	* Determine if a value is a plain Object
	*
	* @param {Object} val The value to test
	* @return {boolean} True if value is a plain Object, otherwise false
	*/
	function isPlainObject(val) {
		if (toString.call(val) !== "[object Object]") return false;
		var prototype = Object.getPrototypeOf(val);
		return prototype === null || prototype === Object.prototype;
	}
	/**
	* Determine if a value is a Date
	*
	* @param {Object} val The value to test
	* @returns {boolean} True if value is a Date, otherwise false
	*/
	function isDate(val) {
		return toString.call(val) === "[object Date]";
	}
	/**
	* Determine if a value is a File
	*
	* @param {Object} val The value to test
	* @returns {boolean} True if value is a File, otherwise false
	*/
	function isFile(val) {
		return toString.call(val) === "[object File]";
	}
	/**
	* Determine if a value is a Blob
	*
	* @param {Object} val The value to test
	* @returns {boolean} True if value is a Blob, otherwise false
	*/
	function isBlob(val) {
		return toString.call(val) === "[object Blob]";
	}
	/**
	* Determine if a value is a Function
	*
	* @param {Object} val The value to test
	* @returns {boolean} True if value is a Function, otherwise false
	*/
	function isFunction(val) {
		return toString.call(val) === "[object Function]";
	}
	/**
	* Determine if a value is a Stream
	*
	* @param {Object} val The value to test
	* @returns {boolean} True if value is a Stream, otherwise false
	*/
	function isStream(val) {
		return isObject(val) && isFunction(val.pipe);
	}
	/**
	* Determine if a value is a URLSearchParams object
	*
	* @param {Object} val The value to test
	* @returns {boolean} True if value is a URLSearchParams object, otherwise false
	*/
	function isURLSearchParams(val) {
		return typeof URLSearchParams !== "undefined" && val instanceof URLSearchParams;
	}
	/**
	* Trim excess whitespace off the beginning and end of a string
	*
	* @param {String} str The String to trim
	* @returns {String} The String freed of excess whitespace
	*/
	function trim(str) {
		return str.trim ? str.trim() : str.replace(/^\s+|\s+$/g, "");
	}
	/**
	* Determine if we're running in a standard browser environment
	*
	* This allows axios to run in a web worker, and react-native.
	* Both environments support XMLHttpRequest, but not fully standard globals.
	*
	* web workers:
	*  typeof window -> undefined
	*  typeof document -> undefined
	*
	* react-native:
	*  navigator.product -> 'ReactNative'
	* nativescript
	*  navigator.product -> 'NativeScript' or 'NS'
	*/
	function isStandardBrowserEnv() {
		if (typeof navigator !== "undefined" && (navigator.product === "ReactNative" || navigator.product === "NativeScript" || navigator.product === "NS")) return false;
		return typeof window !== "undefined" && typeof document !== "undefined";
	}
	/**
	* Iterate over an Array or an Object invoking a function for each item.
	*
	* If `obj` is an Array callback will be called passing
	* the value, index, and complete array for each item.
	*
	* If 'obj' is an Object callback will be called passing
	* the value, key, and complete object for each property.
	*
	* @param {Object|Array} obj The object to iterate
	* @param {Function} fn The callback to invoke for each item
	*/
	function forEach(obj, fn) {
		if (obj === null || typeof obj === "undefined") return;
		if (typeof obj !== "object") obj = [obj];
		if (isArray(obj)) for (var i = 0, l = obj.length; i < l; i++) fn.call(null, obj[i], i, obj);
		else for (var key in obj) if (Object.prototype.hasOwnProperty.call(obj, key)) fn.call(null, obj[key], key, obj);
	}
	/**
	* Accepts varargs expecting each argument to be an object, then
	* immutably merges the properties of each object and returns result.
	*
	* When multiple objects contain the same key the later object in
	* the arguments list will take precedence.
	*
	* Example:
	*
	* ```js
	* var result = merge({foo: 123}, {foo: 456});
	* console.log(result.foo); // outputs 456
	* ```
	*
	* @param {Object} obj1 Object to merge
	* @returns {Object} Result of all merge properties
	*/
	function merge() {
		var result = {};
		function assignValue(val, key) {
			if (isPlainObject(result[key]) && isPlainObject(val)) result[key] = merge(result[key], val);
			else if (isPlainObject(val)) result[key] = merge({}, val);
			else if (isArray(val)) result[key] = val.slice();
			else result[key] = val;
		}
		for (var i = 0, l = arguments.length; i < l; i++) forEach(arguments[i], assignValue);
		return result;
	}
	/**
	* Extends object a by mutably adding to it the properties of object b.
	*
	* @param {Object} a The object to be extended
	* @param {Object} b The object to copy properties from
	* @param {Object} thisArg The object to bind function to
	* @return {Object} The resulting value of object a
	*/
	function extend(a, b, thisArg) {
		forEach(b, function assignValue(val, key) {
			if (thisArg && typeof val === "function") a[key] = bind(val, thisArg);
			else a[key] = val;
		});
		return a;
	}
	/**
	* Remove byte order marker. This catches EF BB BF (the UTF-8 BOM)
	*
	* @param {string} content with BOM
	* @return {string} content value without BOM
	*/
	function stripBOM(content) {
		if (content.charCodeAt(0) === 65279) content = content.slice(1);
		return content;
	}
	module.exports = {
		isArray,
		isArrayBuffer,
		isBuffer,
		isFormData,
		isArrayBufferView,
		isString,
		isNumber,
		isObject,
		isPlainObject,
		isUndefined,
		isDate,
		isFile,
		isBlob,
		isFunction,
		isStream,
		isURLSearchParams,
		isStandardBrowserEnv,
		forEach,
		merge,
		extend,
		trim,
		stripBOM
	};
}));
//#endregion
//#region node_modules/axios/lib/helpers/buildURL.js
var require_buildURL = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var utils = require_utils();
	function encode(val) {
		return encodeURIComponent(val).replace(/%3A/gi, ":").replace(/%24/g, "$").replace(/%2C/gi, ",").replace(/%20/g, "+").replace(/%5B/gi, "[").replace(/%5D/gi, "]");
	}
	/**
	* Build a URL by appending params to the end
	*
	* @param {string} url The base of the url (e.g., http://www.google.com)
	* @param {object} [params] The params to be appended
	* @returns {string} The formatted url
	*/
	module.exports = function buildURL(url, params, paramsSerializer) {
		if (!params) return url;
		var serializedParams;
		if (paramsSerializer) serializedParams = paramsSerializer(params);
		else if (utils.isURLSearchParams(params)) serializedParams = params.toString();
		else {
			var parts = [];
			utils.forEach(params, function serialize(val, key) {
				if (val === null || typeof val === "undefined") return;
				if (utils.isArray(val)) key = key + "[]";
				else val = [val];
				utils.forEach(val, function parseValue(v) {
					if (utils.isDate(v)) v = v.toISOString();
					else if (utils.isObject(v)) v = JSON.stringify(v);
					parts.push(encode(key) + "=" + encode(v));
				});
			});
			serializedParams = parts.join("&");
		}
		if (serializedParams) {
			var hashmarkIndex = url.indexOf("#");
			if (hashmarkIndex !== -1) url = url.slice(0, hashmarkIndex);
			url += (url.indexOf("?") === -1 ? "?" : "&") + serializedParams;
		}
		return url;
	};
}));
//#endregion
//#region node_modules/axios/lib/core/InterceptorManager.js
var require_InterceptorManager = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var utils = require_utils();
	function InterceptorManager() {
		this.handlers = [];
	}
	/**
	* Add a new interceptor to the stack
	*
	* @param {Function} fulfilled The function to handle `then` for a `Promise`
	* @param {Function} rejected The function to handle `reject` for a `Promise`
	*
	* @return {Number} An ID used to remove interceptor later
	*/
	InterceptorManager.prototype.use = function use(fulfilled, rejected, options) {
		this.handlers.push({
			fulfilled,
			rejected,
			synchronous: options ? options.synchronous : false,
			runWhen: options ? options.runWhen : null
		});
		return this.handlers.length - 1;
	};
	/**
	* Remove an interceptor from the stack
	*
	* @param {Number} id The ID that was returned by `use`
	*/
	InterceptorManager.prototype.eject = function eject(id) {
		if (this.handlers[id]) this.handlers[id] = null;
	};
	/**
	* Iterate over all the registered interceptors
	*
	* This method is particularly useful for skipping over any
	* interceptors that may have become `null` calling `eject`.
	*
	* @param {Function} fn The function to call for each interceptor
	*/
	InterceptorManager.prototype.forEach = function forEach(fn) {
		utils.forEach(this.handlers, function forEachHandler(h) {
			if (h !== null) fn(h);
		});
	};
	module.exports = InterceptorManager;
}));
//#endregion
//#region node_modules/axios/lib/helpers/normalizeHeaderName.js
var require_normalizeHeaderName = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var utils = require_utils();
	module.exports = function normalizeHeaderName(headers, normalizedName) {
		utils.forEach(headers, function processHeader(value, name) {
			if (name !== normalizedName && name.toUpperCase() === normalizedName.toUpperCase()) {
				headers[normalizedName] = value;
				delete headers[name];
			}
		});
	};
}));
//#endregion
//#region node_modules/axios/lib/core/enhanceError.js
var require_enhanceError = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	/**
	* Update an Error with the specified config, error code, and response.
	*
	* @param {Error} error The error to update.
	* @param {Object} config The config.
	* @param {string} [code] The error code (for example, 'ECONNABORTED').
	* @param {Object} [request] The request.
	* @param {Object} [response] The response.
	* @returns {Error} The error.
	*/
	module.exports = function enhanceError(error, config, code, request, response) {
		error.config = config;
		if (code) error.code = code;
		error.request = request;
		error.response = response;
		error.isAxiosError = true;
		error.toJSON = function toJSON() {
			return {
				message: this.message,
				name: this.name,
				description: this.description,
				number: this.number,
				fileName: this.fileName,
				lineNumber: this.lineNumber,
				columnNumber: this.columnNumber,
				stack: this.stack,
				config: this.config,
				code: this.code,
				status: this.response && this.response.status ? this.response.status : null
			};
		};
		return error;
	};
}));
//#endregion
//#region node_modules/axios/lib/core/createError.js
var require_createError = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var enhanceError = require_enhanceError();
	/**
	* Create an Error with the specified message, config, error code, request and response.
	*
	* @param {string} message The error message.
	* @param {Object} config The config.
	* @param {string} [code] The error code (for example, 'ECONNABORTED').
	* @param {Object} [request] The request.
	* @param {Object} [response] The response.
	* @returns {Error} The created error.
	*/
	module.exports = function createError(message, config, code, request, response) {
		return enhanceError(new Error(message), config, code, request, response);
	};
}));
//#endregion
//#region node_modules/axios/lib/core/settle.js
var require_settle = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var createError = require_createError();
	/**
	* Resolve or reject a Promise based on response status.
	*
	* @param {Function} resolve A function that resolves the promise.
	* @param {Function} reject A function that rejects the promise.
	* @param {object} response The response.
	*/
	module.exports = function settle(resolve, reject, response) {
		var validateStatus = response.config.validateStatus;
		if (!response.status || !validateStatus || validateStatus(response.status)) resolve(response);
		else reject(createError("Request failed with status code " + response.status, response.config, null, response.request, response));
	};
}));
//#endregion
//#region node_modules/axios/lib/helpers/cookies.js
var require_cookies = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var utils = require_utils();
	module.exports = utils.isStandardBrowserEnv() ? (function standardBrowserEnv() {
		return {
			write: function write(name, value, expires, path, domain, secure) {
				var cookie = [];
				cookie.push(name + "=" + encodeURIComponent(value));
				if (utils.isNumber(expires)) cookie.push("expires=" + new Date(expires).toGMTString());
				if (utils.isString(path)) cookie.push("path=" + path);
				if (utils.isString(domain)) cookie.push("domain=" + domain);
				if (secure === true) cookie.push("secure");
				document.cookie = cookie.join("; ");
			},
			read: function read(name) {
				var match = document.cookie.match(new RegExp("(^|;\\s*)(" + name + ")=([^;]*)"));
				return match ? decodeURIComponent(match[3]) : null;
			},
			remove: function remove(name) {
				this.write(name, "", Date.now() - 864e5);
			}
		};
	})() : (function nonStandardBrowserEnv() {
		return {
			write: function write() {},
			read: function read() {
				return null;
			},
			remove: function remove() {}
		};
	})();
}));
//#endregion
//#region node_modules/axios/lib/helpers/isAbsoluteURL.js
var require_isAbsoluteURL = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	/**
	* Determines whether the specified URL is absolute
	*
	* @param {string} url The URL to test
	* @returns {boolean} True if the specified URL is absolute, otherwise false
	*/
	module.exports = function isAbsoluteURL(url) {
		return /^([a-z][a-z\d\+\-\.]*:)?\/\//i.test(url);
	};
}));
//#endregion
//#region node_modules/axios/lib/helpers/combineURLs.js
var require_combineURLs = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	/**
	* Creates a new URL by combining the specified URLs
	*
	* @param {string} baseURL The base URL
	* @param {string} relativeURL The relative URL
	* @returns {string} The combined URL
	*/
	module.exports = function combineURLs(baseURL, relativeURL) {
		return relativeURL ? baseURL.replace(/\/+$/, "") + "/" + relativeURL.replace(/^\/+/, "") : baseURL;
	};
}));
//#endregion
//#region node_modules/axios/lib/core/buildFullPath.js
var require_buildFullPath = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var isAbsoluteURL = require_isAbsoluteURL();
	var combineURLs = require_combineURLs();
	/**
	* Creates a new URL by combining the baseURL with the requestedURL,
	* only when the requestedURL is not already an absolute URL.
	* If the requestURL is absolute, this function returns the requestedURL untouched.
	*
	* @param {string} baseURL The base URL
	* @param {string} requestedURL Absolute or relative URL to combine
	* @returns {string} The combined full path
	*/
	module.exports = function buildFullPath(baseURL, requestedURL) {
		if (baseURL && !isAbsoluteURL(requestedURL)) return combineURLs(baseURL, requestedURL);
		return requestedURL;
	};
}));
//#endregion
//#region node_modules/axios/lib/helpers/parseHeaders.js
var require_parseHeaders = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var utils = require_utils();
	var ignoreDuplicateOf = [
		"age",
		"authorization",
		"content-length",
		"content-type",
		"etag",
		"expires",
		"from",
		"host",
		"if-modified-since",
		"if-unmodified-since",
		"last-modified",
		"location",
		"max-forwards",
		"proxy-authorization",
		"referer",
		"retry-after",
		"user-agent"
	];
	/**
	* Parse headers into an object
	*
	* ```
	* Date: Wed, 27 Aug 2014 08:58:49 GMT
	* Content-Type: application/json
	* Connection: keep-alive
	* Transfer-Encoding: chunked
	* ```
	*
	* @param {String} headers Headers needing to be parsed
	* @returns {Object} Headers parsed into an object
	*/
	module.exports = function parseHeaders(headers) {
		var parsed = {};
		var key;
		var val;
		var i;
		if (!headers) return parsed;
		utils.forEach(headers.split("\n"), function parser(line) {
			i = line.indexOf(":");
			key = utils.trim(line.substr(0, i)).toLowerCase();
			val = utils.trim(line.substr(i + 1));
			if (key) {
				if (parsed[key] && ignoreDuplicateOf.indexOf(key) >= 0) return;
				if (key === "set-cookie") parsed[key] = (parsed[key] ? parsed[key] : []).concat([val]);
				else parsed[key] = parsed[key] ? parsed[key] + ", " + val : val;
			}
		});
		return parsed;
	};
}));
//#endregion
//#region node_modules/axios/lib/helpers/isURLSameOrigin.js
var require_isURLSameOrigin = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var utils = require_utils();
	module.exports = utils.isStandardBrowserEnv() ? (function standardBrowserEnv() {
		var msie = /(msie|trident)/i.test(navigator.userAgent);
		var urlParsingNode = document.createElement("a");
		var originURL;
		/**
		* Parse a URL to discover it's components
		*
		* @param {String} url The URL to be parsed
		* @returns {Object}
		*/
		function resolveURL(url) {
			var href = url;
			if (msie) {
				urlParsingNode.setAttribute("href", href);
				href = urlParsingNode.href;
			}
			urlParsingNode.setAttribute("href", href);
			return {
				href: urlParsingNode.href,
				protocol: urlParsingNode.protocol ? urlParsingNode.protocol.replace(/:$/, "") : "",
				host: urlParsingNode.host,
				search: urlParsingNode.search ? urlParsingNode.search.replace(/^\?/, "") : "",
				hash: urlParsingNode.hash ? urlParsingNode.hash.replace(/^#/, "") : "",
				hostname: urlParsingNode.hostname,
				port: urlParsingNode.port,
				pathname: urlParsingNode.pathname.charAt(0) === "/" ? urlParsingNode.pathname : "/" + urlParsingNode.pathname
			};
		}
		originURL = resolveURL(window.location.href);
		/**
		* Determine if a URL shares the same origin as the current location
		*
		* @param {String} requestURL The URL to test
		* @returns {boolean} True if URL shares the same origin, otherwise false
		*/
		return function isURLSameOrigin(requestURL) {
			var parsed = utils.isString(requestURL) ? resolveURL(requestURL) : requestURL;
			return parsed.protocol === originURL.protocol && parsed.host === originURL.host;
		};
	})() : (function nonStandardBrowserEnv() {
		return function isURLSameOrigin() {
			return true;
		};
	})();
}));
//#endregion
//#region node_modules/axios/lib/cancel/Cancel.js
var require_Cancel = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	/**
	* A `Cancel` is an object that is thrown when an operation is canceled.
	*
	* @class
	* @param {string=} message The message.
	*/
	function Cancel(message) {
		this.message = message;
	}
	Cancel.prototype.toString = function toString() {
		return "Cancel" + (this.message ? ": " + this.message : "");
	};
	Cancel.prototype.__CANCEL__ = true;
	module.exports = Cancel;
}));
//#endregion
//#region node_modules/axios/lib/adapters/xhr.js
var require_xhr = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var utils = require_utils();
	var settle = require_settle();
	var cookies = require_cookies();
	var buildURL = require_buildURL();
	var buildFullPath = require_buildFullPath();
	var parseHeaders = require_parseHeaders();
	var isURLSameOrigin = require_isURLSameOrigin();
	var createError = require_createError();
	var defaults = require_defaults();
	var Cancel = require_Cancel();
	module.exports = function xhrAdapter(config) {
		return new Promise(function dispatchXhrRequest(resolve, reject) {
			var requestData = config.data;
			var requestHeaders = config.headers;
			var responseType = config.responseType;
			var onCanceled;
			function done() {
				if (config.cancelToken) config.cancelToken.unsubscribe(onCanceled);
				if (config.signal) config.signal.removeEventListener("abort", onCanceled);
			}
			if (utils.isFormData(requestData)) delete requestHeaders["Content-Type"];
			var request = new XMLHttpRequest();
			if (config.auth) {
				var username = config.auth.username || "";
				var password = config.auth.password ? unescape(encodeURIComponent(config.auth.password)) : "";
				requestHeaders.Authorization = "Basic " + btoa(username + ":" + password);
			}
			var fullPath = buildFullPath(config.baseURL, config.url);
			request.open(config.method.toUpperCase(), buildURL(fullPath, config.params, config.paramsSerializer), true);
			request.timeout = config.timeout;
			function onloadend() {
				if (!request) return;
				var responseHeaders = "getAllResponseHeaders" in request ? parseHeaders(request.getAllResponseHeaders()) : null;
				settle(function _resolve(value) {
					resolve(value);
					done();
				}, function _reject(err) {
					reject(err);
					done();
				}, {
					data: !responseType || responseType === "text" || responseType === "json" ? request.responseText : request.response,
					status: request.status,
					statusText: request.statusText,
					headers: responseHeaders,
					config,
					request
				});
				request = null;
			}
			if ("onloadend" in request) request.onloadend = onloadend;
			else request.onreadystatechange = function handleLoad() {
				if (!request || request.readyState !== 4) return;
				if (request.status === 0 && !(request.responseURL && request.responseURL.indexOf("file:") === 0)) return;
				setTimeout(onloadend);
			};
			request.onabort = function handleAbort() {
				if (!request) return;
				reject(createError("Request aborted", config, "ECONNABORTED", request));
				request = null;
			};
			request.onerror = function handleError() {
				reject(createError("Network Error", config, null, request));
				request = null;
			};
			request.ontimeout = function handleTimeout() {
				var timeoutErrorMessage = config.timeout ? "timeout of " + config.timeout + "ms exceeded" : "timeout exceeded";
				var transitional = config.transitional || defaults.transitional;
				if (config.timeoutErrorMessage) timeoutErrorMessage = config.timeoutErrorMessage;
				reject(createError(timeoutErrorMessage, config, transitional.clarifyTimeoutError ? "ETIMEDOUT" : "ECONNABORTED", request));
				request = null;
			};
			if (utils.isStandardBrowserEnv()) {
				var xsrfValue = (config.withCredentials || isURLSameOrigin(fullPath)) && config.xsrfCookieName ? cookies.read(config.xsrfCookieName) : void 0;
				if (xsrfValue) requestHeaders[config.xsrfHeaderName] = xsrfValue;
			}
			if ("setRequestHeader" in request) utils.forEach(requestHeaders, function setRequestHeader(val, key) {
				if (typeof requestData === "undefined" && key.toLowerCase() === "content-type") delete requestHeaders[key];
				else request.setRequestHeader(key, val);
			});
			if (!utils.isUndefined(config.withCredentials)) request.withCredentials = !!config.withCredentials;
			if (responseType && responseType !== "json") request.responseType = config.responseType;
			if (typeof config.onDownloadProgress === "function") request.addEventListener("progress", config.onDownloadProgress);
			if (typeof config.onUploadProgress === "function" && request.upload) request.upload.addEventListener("progress", config.onUploadProgress);
			if (config.cancelToken || config.signal) {
				onCanceled = function(cancel) {
					if (!request) return;
					reject(!cancel || cancel && cancel.type ? new Cancel("canceled") : cancel);
					request.abort();
					request = null;
				};
				config.cancelToken && config.cancelToken.subscribe(onCanceled);
				if (config.signal) config.signal.aborted ? onCanceled() : config.signal.addEventListener("abort", onCanceled);
			}
			if (!requestData) requestData = null;
			request.send(requestData);
		});
	};
}));
//#endregion
//#region node_modules/axios/lib/defaults.js
var require_defaults = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var utils = require_utils();
	var normalizeHeaderName = require_normalizeHeaderName();
	var enhanceError = require_enhanceError();
	var DEFAULT_CONTENT_TYPE = { "Content-Type": "application/x-www-form-urlencoded" };
	function setContentTypeIfUnset(headers, value) {
		if (!utils.isUndefined(headers) && utils.isUndefined(headers["Content-Type"])) headers["Content-Type"] = value;
	}
	function getDefaultAdapter() {
		var adapter;
		if (typeof XMLHttpRequest !== "undefined") adapter = require_xhr();
		else if (typeof process !== "undefined" && Object.prototype.toString.call(process) === "[object process]") adapter = require_xhr();
		return adapter;
	}
	function stringifySafely(rawValue, parser, encoder) {
		if (utils.isString(rawValue)) try {
			(parser || JSON.parse)(rawValue);
			return utils.trim(rawValue);
		} catch (e) {
			if (e.name !== "SyntaxError") throw e;
		}
		return (encoder || JSON.stringify)(rawValue);
	}
	var defaults = {
		transitional: {
			silentJSONParsing: true,
			forcedJSONParsing: true,
			clarifyTimeoutError: false
		},
		adapter: getDefaultAdapter(),
		transformRequest: [function transformRequest(data, headers) {
			normalizeHeaderName(headers, "Accept");
			normalizeHeaderName(headers, "Content-Type");
			if (utils.isFormData(data) || utils.isArrayBuffer(data) || utils.isBuffer(data) || utils.isStream(data) || utils.isFile(data) || utils.isBlob(data)) return data;
			if (utils.isArrayBufferView(data)) return data.buffer;
			if (utils.isURLSearchParams(data)) {
				setContentTypeIfUnset(headers, "application/x-www-form-urlencoded;charset=utf-8");
				return data.toString();
			}
			if (utils.isObject(data) || headers && headers["Content-Type"] === "application/json") {
				setContentTypeIfUnset(headers, "application/json");
				return stringifySafely(data);
			}
			return data;
		}],
		transformResponse: [function transformResponse(data) {
			var transitional = this.transitional || defaults.transitional;
			var silentJSONParsing = transitional && transitional.silentJSONParsing;
			var forcedJSONParsing = transitional && transitional.forcedJSONParsing;
			var strictJSONParsing = !silentJSONParsing && this.responseType === "json";
			if (strictJSONParsing || forcedJSONParsing && utils.isString(data) && data.length) try {
				return JSON.parse(data);
			} catch (e) {
				if (strictJSONParsing) {
					if (e.name === "SyntaxError") throw enhanceError(e, this, "E_JSON_PARSE");
					throw e;
				}
			}
			return data;
		}],
		/**
		* A timeout in milliseconds to abort a request. If set to 0 (default) a
		* timeout is not created.
		*/
		timeout: 0,
		xsrfCookieName: "XSRF-TOKEN",
		xsrfHeaderName: "X-XSRF-TOKEN",
		maxContentLength: -1,
		maxBodyLength: -1,
		validateStatus: function validateStatus(status) {
			return status >= 200 && status < 300;
		},
		headers: { common: { "Accept": "application/json, text/plain, */*" } }
	};
	utils.forEach([
		"delete",
		"get",
		"head"
	], function forEachMethodNoData(method) {
		defaults.headers[method] = {};
	});
	utils.forEach([
		"post",
		"put",
		"patch"
	], function forEachMethodWithData(method) {
		defaults.headers[method] = utils.merge(DEFAULT_CONTENT_TYPE);
	});
	module.exports = defaults;
}));
//#endregion
//#region node_modules/axios/lib/core/transformData.js
var require_transformData = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var utils = require_utils();
	var defaults = require_defaults();
	/**
	* Transform the data for a request or a response
	*
	* @param {Object|String} data The data to be transformed
	* @param {Array} headers The headers for the request or response
	* @param {Array|Function} fns A single function or Array of functions
	* @returns {*} The resulting transformed data
	*/
	module.exports = function transformData(data, headers, fns) {
		var context = this || defaults;
		utils.forEach(fns, function transform(fn) {
			data = fn.call(context, data, headers);
		});
		return data;
	};
}));
//#endregion
//#region node_modules/axios/lib/cancel/isCancel.js
var require_isCancel = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = function isCancel(value) {
		return !!(value && value.__CANCEL__);
	};
}));
//#endregion
//#region node_modules/axios/lib/core/dispatchRequest.js
var require_dispatchRequest = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var utils = require_utils();
	var transformData = require_transformData();
	var isCancel = require_isCancel();
	var defaults = require_defaults();
	var Cancel = require_Cancel();
	/**
	* Throws a `Cancel` if cancellation has been requested.
	*/
	function throwIfCancellationRequested(config) {
		if (config.cancelToken) config.cancelToken.throwIfRequested();
		if (config.signal && config.signal.aborted) throw new Cancel("canceled");
	}
	/**
	* Dispatch a request to the server using the configured adapter.
	*
	* @param {object} config The config that is to be used for the request
	* @returns {Promise} The Promise to be fulfilled
	*/
	module.exports = function dispatchRequest(config) {
		throwIfCancellationRequested(config);
		config.headers = config.headers || {};
		config.data = transformData.call(config, config.data, config.headers, config.transformRequest);
		config.headers = utils.merge(config.headers.common || {}, config.headers[config.method] || {}, config.headers);
		utils.forEach([
			"delete",
			"get",
			"head",
			"post",
			"put",
			"patch",
			"common"
		], function cleanHeaderConfig(method) {
			delete config.headers[method];
		});
		return (config.adapter || defaults.adapter)(config).then(function onAdapterResolution(response) {
			throwIfCancellationRequested(config);
			response.data = transformData.call(config, response.data, response.headers, config.transformResponse);
			return response;
		}, function onAdapterRejection(reason) {
			if (!isCancel(reason)) {
				throwIfCancellationRequested(config);
				if (reason && reason.response) reason.response.data = transformData.call(config, reason.response.data, reason.response.headers, config.transformResponse);
			}
			return Promise.reject(reason);
		});
	};
}));
//#endregion
//#region node_modules/axios/lib/core/mergeConfig.js
var require_mergeConfig = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var utils = require_utils();
	/**
	* Config-specific merge-function which creates a new config-object
	* by merging two configuration objects together.
	*
	* @param {Object} config1
	* @param {Object} config2
	* @returns {Object} New object resulting from merging config2 to config1
	*/
	module.exports = function mergeConfig(config1, config2) {
		config2 = config2 || {};
		var config = {};
		function getMergedValue(target, source) {
			if (utils.isPlainObject(target) && utils.isPlainObject(source)) return utils.merge(target, source);
			else if (utils.isPlainObject(source)) return utils.merge({}, source);
			else if (utils.isArray(source)) return source.slice();
			return source;
		}
		function mergeDeepProperties(prop) {
			if (!utils.isUndefined(config2[prop])) return getMergedValue(config1[prop], config2[prop]);
			else if (!utils.isUndefined(config1[prop])) return getMergedValue(void 0, config1[prop]);
		}
		function valueFromConfig2(prop) {
			if (!utils.isUndefined(config2[prop])) return getMergedValue(void 0, config2[prop]);
		}
		function defaultToConfig2(prop) {
			if (!utils.isUndefined(config2[prop])) return getMergedValue(void 0, config2[prop]);
			else if (!utils.isUndefined(config1[prop])) return getMergedValue(void 0, config1[prop]);
		}
		function mergeDirectKeys(prop) {
			if (prop in config2) return getMergedValue(config1[prop], config2[prop]);
			else if (prop in config1) return getMergedValue(void 0, config1[prop]);
		}
		var mergeMap = {
			"url": valueFromConfig2,
			"method": valueFromConfig2,
			"data": valueFromConfig2,
			"baseURL": defaultToConfig2,
			"transformRequest": defaultToConfig2,
			"transformResponse": defaultToConfig2,
			"paramsSerializer": defaultToConfig2,
			"timeout": defaultToConfig2,
			"timeoutMessage": defaultToConfig2,
			"withCredentials": defaultToConfig2,
			"adapter": defaultToConfig2,
			"responseType": defaultToConfig2,
			"xsrfCookieName": defaultToConfig2,
			"xsrfHeaderName": defaultToConfig2,
			"onUploadProgress": defaultToConfig2,
			"onDownloadProgress": defaultToConfig2,
			"decompress": defaultToConfig2,
			"maxContentLength": defaultToConfig2,
			"maxBodyLength": defaultToConfig2,
			"transport": defaultToConfig2,
			"httpAgent": defaultToConfig2,
			"httpsAgent": defaultToConfig2,
			"cancelToken": defaultToConfig2,
			"socketPath": defaultToConfig2,
			"responseEncoding": defaultToConfig2,
			"validateStatus": mergeDirectKeys
		};
		utils.forEach(Object.keys(config1).concat(Object.keys(config2)), function computeConfigValue(prop) {
			var merge = mergeMap[prop] || mergeDeepProperties;
			var configValue = merge(prop);
			utils.isUndefined(configValue) && merge !== mergeDirectKeys || (config[prop] = configValue);
		});
		return config;
	};
}));
//#endregion
//#region node_modules/axios/lib/env/data.js
var require_data = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = { "version": "0.24.0" };
}));
//#endregion
//#region node_modules/axios/lib/helpers/validator.js
var require_validator = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var VERSION = require_data().version;
	var validators = {};
	[
		"object",
		"boolean",
		"number",
		"function",
		"string",
		"symbol"
	].forEach(function(type, i) {
		validators[type] = function validator(thing) {
			return typeof thing === type || "a" + (i < 1 ? "n " : " ") + type;
		};
	});
	var deprecatedWarnings = {};
	/**
	* Transitional option validator
	* @param {function|boolean?} validator - set to false if the transitional option has been removed
	* @param {string?} version - deprecated version / removed since version
	* @param {string?} message - some message with additional info
	* @returns {function}
	*/
	validators.transitional = function transitional(validator, version, message) {
		function formatMessage(opt, desc) {
			return "[Axios v" + VERSION + "] Transitional option '" + opt + "'" + desc + (message ? ". " + message : "");
		}
		return function(value, opt, opts) {
			if (validator === false) throw new Error(formatMessage(opt, " has been removed" + (version ? " in " + version : "")));
			if (version && !deprecatedWarnings[opt]) {
				deprecatedWarnings[opt] = true;
				console.warn(formatMessage(opt, " has been deprecated since v" + version + " and will be removed in the near future"));
			}
			return validator ? validator(value, opt, opts) : true;
		};
	};
	/**
	* Assert object's properties type
	* @param {object} options
	* @param {object} schema
	* @param {boolean?} allowUnknown
	*/
	function assertOptions(options, schema, allowUnknown) {
		if (typeof options !== "object") throw new TypeError("options must be an object");
		var keys = Object.keys(options);
		var i = keys.length;
		while (i-- > 0) {
			var opt = keys[i];
			var validator = schema[opt];
			if (validator) {
				var value = options[opt];
				var result = value === void 0 || validator(value, opt, options);
				if (result !== true) throw new TypeError("option " + opt + " must be " + result);
				continue;
			}
			if (allowUnknown !== true) throw Error("Unknown option " + opt);
		}
	}
	module.exports = {
		assertOptions,
		validators
	};
}));
//#endregion
//#region node_modules/axios/lib/core/Axios.js
var require_Axios = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var utils = require_utils();
	var buildURL = require_buildURL();
	var InterceptorManager = require_InterceptorManager();
	var dispatchRequest = require_dispatchRequest();
	var mergeConfig = require_mergeConfig();
	var validator = require_validator();
	var validators = validator.validators;
	/**
	* Create a new instance of Axios
	*
	* @param {Object} instanceConfig The default config for the instance
	*/
	function Axios(instanceConfig) {
		this.defaults = instanceConfig;
		this.interceptors = {
			request: new InterceptorManager(),
			response: new InterceptorManager()
		};
	}
	/**
	* Dispatch a request
	*
	* @param {Object} config The config specific for this request (merged with this.defaults)
	*/
	Axios.prototype.request = function request(config) {
		if (typeof config === "string") {
			config = arguments[1] || {};
			config.url = arguments[0];
		} else config = config || {};
		config = mergeConfig(this.defaults, config);
		if (config.method) config.method = config.method.toLowerCase();
		else if (this.defaults.method) config.method = this.defaults.method.toLowerCase();
		else config.method = "get";
		var transitional = config.transitional;
		if (transitional !== void 0) validator.assertOptions(transitional, {
			silentJSONParsing: validators.transitional(validators.boolean),
			forcedJSONParsing: validators.transitional(validators.boolean),
			clarifyTimeoutError: validators.transitional(validators.boolean)
		}, false);
		var requestInterceptorChain = [];
		var synchronousRequestInterceptors = true;
		this.interceptors.request.forEach(function unshiftRequestInterceptors(interceptor) {
			if (typeof interceptor.runWhen === "function" && interceptor.runWhen(config) === false) return;
			synchronousRequestInterceptors = synchronousRequestInterceptors && interceptor.synchronous;
			requestInterceptorChain.unshift(interceptor.fulfilled, interceptor.rejected);
		});
		var responseInterceptorChain = [];
		this.interceptors.response.forEach(function pushResponseInterceptors(interceptor) {
			responseInterceptorChain.push(interceptor.fulfilled, interceptor.rejected);
		});
		var promise;
		if (!synchronousRequestInterceptors) {
			var chain = [dispatchRequest, void 0];
			Array.prototype.unshift.apply(chain, requestInterceptorChain);
			chain = chain.concat(responseInterceptorChain);
			promise = Promise.resolve(config);
			while (chain.length) promise = promise.then(chain.shift(), chain.shift());
			return promise;
		}
		var newConfig = config;
		while (requestInterceptorChain.length) {
			var onFulfilled = requestInterceptorChain.shift();
			var onRejected = requestInterceptorChain.shift();
			try {
				newConfig = onFulfilled(newConfig);
			} catch (error) {
				onRejected(error);
				break;
			}
		}
		try {
			promise = dispatchRequest(newConfig);
		} catch (error) {
			return Promise.reject(error);
		}
		while (responseInterceptorChain.length) promise = promise.then(responseInterceptorChain.shift(), responseInterceptorChain.shift());
		return promise;
	};
	Axios.prototype.getUri = function getUri(config) {
		config = mergeConfig(this.defaults, config);
		return buildURL(config.url, config.params, config.paramsSerializer).replace(/^\?/, "");
	};
	utils.forEach([
		"delete",
		"get",
		"head",
		"options"
	], function forEachMethodNoData(method) {
		Axios.prototype[method] = function(url, config) {
			return this.request(mergeConfig(config || {}, {
				method,
				url,
				data: (config || {}).data
			}));
		};
	});
	utils.forEach([
		"post",
		"put",
		"patch"
	], function forEachMethodWithData(method) {
		Axios.prototype[method] = function(url, data, config) {
			return this.request(mergeConfig(config || {}, {
				method,
				url,
				data
			}));
		};
	});
	module.exports = Axios;
}));
//#endregion
//#region node_modules/axios/lib/cancel/CancelToken.js
var require_CancelToken = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var Cancel = require_Cancel();
	/**
	* A `CancelToken` is an object that can be used to request cancellation of an operation.
	*
	* @class
	* @param {Function} executor The executor function.
	*/
	function CancelToken(executor) {
		if (typeof executor !== "function") throw new TypeError("executor must be a function.");
		var resolvePromise;
		this.promise = new Promise(function promiseExecutor(resolve) {
			resolvePromise = resolve;
		});
		var token = this;
		this.promise.then(function(cancel) {
			if (!token._listeners) return;
			var i;
			var l = token._listeners.length;
			for (i = 0; i < l; i++) token._listeners[i](cancel);
			token._listeners = null;
		});
		this.promise.then = function(onfulfilled) {
			var _resolve;
			var promise = new Promise(function(resolve) {
				token.subscribe(resolve);
				_resolve = resolve;
			}).then(onfulfilled);
			promise.cancel = function reject() {
				token.unsubscribe(_resolve);
			};
			return promise;
		};
		executor(function cancel(message) {
			if (token.reason) return;
			token.reason = new Cancel(message);
			resolvePromise(token.reason);
		});
	}
	/**
	* Throws a `Cancel` if cancellation has been requested.
	*/
	CancelToken.prototype.throwIfRequested = function throwIfRequested() {
		if (this.reason) throw this.reason;
	};
	/**
	* Subscribe to the cancel signal
	*/
	CancelToken.prototype.subscribe = function subscribe(listener) {
		if (this.reason) {
			listener(this.reason);
			return;
		}
		if (this._listeners) this._listeners.push(listener);
		else this._listeners = [listener];
	};
	/**
	* Unsubscribe from the cancel signal
	*/
	CancelToken.prototype.unsubscribe = function unsubscribe(listener) {
		if (!this._listeners) return;
		var index = this._listeners.indexOf(listener);
		if (index !== -1) this._listeners.splice(index, 1);
	};
	/**
	* Returns an object that contains a new `CancelToken` and a function that, when called,
	* cancels the `CancelToken`.
	*/
	CancelToken.source = function source() {
		var cancel;
		return {
			token: new CancelToken(function executor(c) {
				cancel = c;
			}),
			cancel
		};
	};
	module.exports = CancelToken;
}));
//#endregion
//#region node_modules/axios/lib/helpers/spread.js
var require_spread = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	/**
	* Syntactic sugar for invoking a function and expanding an array for arguments.
	*
	* Common use case would be to use `Function.prototype.apply`.
	*
	*  ```js
	*  function f(x, y, z) {}
	*  var args = [1, 2, 3];
	*  f.apply(null, args);
	*  ```
	*
	* With `spread` this example can be re-written.
	*
	*  ```js
	*  spread(function(x, y, z) {})([1, 2, 3]);
	*  ```
	*
	* @param {Function} callback
	* @returns {Function}
	*/
	module.exports = function spread(callback) {
		return function wrap(arr) {
			return callback.apply(null, arr);
		};
	};
}));
//#endregion
//#region node_modules/axios/lib/helpers/isAxiosError.js
var require_isAxiosError = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	/**
	* Determines whether the payload is an error thrown by Axios
	*
	* @param {*} payload The value to test
	* @returns {boolean} True if the payload is an error thrown by Axios, otherwise false
	*/
	module.exports = function isAxiosError(payload) {
		return typeof payload === "object" && payload.isAxiosError === true;
	};
}));
//#endregion
//#region node_modules/axios/lib/axios.js
var require_axios$1 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var utils = require_utils();
	var bind = require_bind();
	var Axios = require_Axios();
	var mergeConfig = require_mergeConfig();
	var defaults = require_defaults();
	/**
	* Create an instance of Axios
	*
	* @param {Object} defaultConfig The default config for the instance
	* @return {Axios} A new instance of Axios
	*/
	function createInstance(defaultConfig) {
		var context = new Axios(defaultConfig);
		var instance = bind(Axios.prototype.request, context);
		utils.extend(instance, Axios.prototype, context);
		utils.extend(instance, context);
		instance.create = function create(instanceConfig) {
			return createInstance(mergeConfig(defaultConfig, instanceConfig));
		};
		return instance;
	}
	var axios = createInstance(defaults);
	axios.Axios = Axios;
	axios.Cancel = require_Cancel();
	axios.CancelToken = require_CancelToken();
	axios.isCancel = require_isCancel();
	axios.VERSION = require_data().version;
	axios.all = function all(promises) {
		return Promise.all(promises);
	};
	axios.spread = require_spread();
	axios.isAxiosError = require_isAxiosError();
	module.exports = axios;
	module.exports.default = axios;
}));
//#endregion
//#region node_modules/axios/index.js
var require_axios = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require_axios$1();
}));
//#endregion
//#region node_modules/crypt/crypt.js
var require_crypt = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	(function() {
		var base64map = "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/", crypt = {
			rotl: function(n, b) {
				return n << b | n >>> 32 - b;
			},
			rotr: function(n, b) {
				return n << 32 - b | n >>> b;
			},
			endian: function(n) {
				if (n.constructor == Number) return crypt.rotl(n, 8) & 16711935 | crypt.rotl(n, 24) & 4278255360;
				for (var i = 0; i < n.length; i++) n[i] = crypt.endian(n[i]);
				return n;
			},
			randomBytes: function(n) {
				for (var bytes = []; n > 0; n--) bytes.push(Math.floor(Math.random() * 256));
				return bytes;
			},
			bytesToWords: function(bytes) {
				for (var words = [], i = 0, b = 0; i < bytes.length; i++, b += 8) words[b >>> 5] |= bytes[i] << 24 - b % 32;
				return words;
			},
			wordsToBytes: function(words) {
				for (var bytes = [], b = 0; b < words.length * 32; b += 8) bytes.push(words[b >>> 5] >>> 24 - b % 32 & 255);
				return bytes;
			},
			bytesToHex: function(bytes) {
				for (var hex = [], i = 0; i < bytes.length; i++) {
					hex.push((bytes[i] >>> 4).toString(16));
					hex.push((bytes[i] & 15).toString(16));
				}
				return hex.join("");
			},
			hexToBytes: function(hex) {
				for (var bytes = [], c = 0; c < hex.length; c += 2) bytes.push(parseInt(hex.substr(c, 2), 16));
				return bytes;
			},
			bytesToBase64: function(bytes) {
				for (var base64 = [], i = 0; i < bytes.length; i += 3) {
					var triplet = bytes[i] << 16 | bytes[i + 1] << 8 | bytes[i + 2];
					for (var j = 0; j < 4; j++) if (i * 8 + j * 6 <= bytes.length * 8) base64.push(base64map.charAt(triplet >>> 6 * (3 - j) & 63));
					else base64.push("=");
				}
				return base64.join("");
			},
			base64ToBytes: function(base64) {
				base64 = base64.replace(/[^A-Z0-9+\/]/gi, "");
				for (var bytes = [], i = 0, imod4 = 0; i < base64.length; imod4 = ++i % 4) {
					if (imod4 == 0) continue;
					bytes.push((base64map.indexOf(base64.charAt(i - 1)) & Math.pow(2, -2 * imod4 + 8) - 1) << imod4 * 2 | base64map.indexOf(base64.charAt(i)) >>> 6 - imod4 * 2);
				}
				return bytes;
			}
		};
		module.exports = crypt;
	})();
}));
//#endregion
//#region node_modules/charenc/charenc.js
var require_charenc = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var charenc = {
		utf8: {
			stringToBytes: function(str) {
				return charenc.bin.stringToBytes(unescape(encodeURIComponent(str)));
			},
			bytesToString: function(bytes) {
				return decodeURIComponent(escape(charenc.bin.bytesToString(bytes)));
			}
		},
		bin: {
			stringToBytes: function(str) {
				for (var bytes = [], i = 0; i < str.length; i++) bytes.push(str.charCodeAt(i) & 255);
				return bytes;
			},
			bytesToString: function(bytes) {
				for (var str = [], i = 0; i < bytes.length; i++) str.push(String.fromCharCode(bytes[i]));
				return str.join("");
			}
		}
	};
	module.exports = charenc;
}));
//#endregion
//#region node_modules/is-buffer/index.js
var require_is_buffer = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	/*!
	* Determine if an object is a Buffer
	*
	* @author   Feross Aboukhadijeh <https://feross.org>
	* @license  MIT
	*/
	module.exports = function(obj) {
		return obj != null && (isBuffer(obj) || isSlowBuffer(obj) || !!obj._isBuffer);
	};
	function isBuffer(obj) {
		return !!obj.constructor && typeof obj.constructor.isBuffer === "function" && obj.constructor.isBuffer(obj);
	}
	function isSlowBuffer(obj) {
		return typeof obj.readFloatLE === "function" && typeof obj.slice === "function" && isBuffer(obj.slice(0, 0));
	}
}));
//#endregion
//#region node_modules/md5/md5.js
var require_md5 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	(function() {
		var crypt = require_crypt(), utf8 = require_charenc().utf8, isBuffer = require_is_buffer(), bin = require_charenc().bin, md5 = function(message, options) {
			if (message.constructor == String) if (options && options.encoding === "binary") message = bin.stringToBytes(message);
			else message = utf8.stringToBytes(message);
			else if (isBuffer(message)) message = Array.prototype.slice.call(message, 0);
			else if (!Array.isArray(message) && message.constructor !== Uint8Array) message = message.toString();
			var m = crypt.bytesToWords(message), l = message.length * 8, a = 1732584193, b = -271733879, c = -1732584194, d = 271733878;
			for (var i = 0; i < m.length; i++) m[i] = (m[i] << 8 | m[i] >>> 24) & 16711935 | (m[i] << 24 | m[i] >>> 8) & 4278255360;
			m[l >>> 5] |= 128 << l % 32;
			m[(l + 64 >>> 9 << 4) + 14] = l;
			var FF = md5._ff, GG = md5._gg, HH = md5._hh, II = md5._ii;
			for (var i = 0; i < m.length; i += 16) {
				var aa = a, bb = b, cc = c, dd = d;
				a = FF(a, b, c, d, m[i + 0], 7, -680876936);
				d = FF(d, a, b, c, m[i + 1], 12, -389564586);
				c = FF(c, d, a, b, m[i + 2], 17, 606105819);
				b = FF(b, c, d, a, m[i + 3], 22, -1044525330);
				a = FF(a, b, c, d, m[i + 4], 7, -176418897);
				d = FF(d, a, b, c, m[i + 5], 12, 1200080426);
				c = FF(c, d, a, b, m[i + 6], 17, -1473231341);
				b = FF(b, c, d, a, m[i + 7], 22, -45705983);
				a = FF(a, b, c, d, m[i + 8], 7, 1770035416);
				d = FF(d, a, b, c, m[i + 9], 12, -1958414417);
				c = FF(c, d, a, b, m[i + 10], 17, -42063);
				b = FF(b, c, d, a, m[i + 11], 22, -1990404162);
				a = FF(a, b, c, d, m[i + 12], 7, 1804603682);
				d = FF(d, a, b, c, m[i + 13], 12, -40341101);
				c = FF(c, d, a, b, m[i + 14], 17, -1502002290);
				b = FF(b, c, d, a, m[i + 15], 22, 1236535329);
				a = GG(a, b, c, d, m[i + 1], 5, -165796510);
				d = GG(d, a, b, c, m[i + 6], 9, -1069501632);
				c = GG(c, d, a, b, m[i + 11], 14, 643717713);
				b = GG(b, c, d, a, m[i + 0], 20, -373897302);
				a = GG(a, b, c, d, m[i + 5], 5, -701558691);
				d = GG(d, a, b, c, m[i + 10], 9, 38016083);
				c = GG(c, d, a, b, m[i + 15], 14, -660478335);
				b = GG(b, c, d, a, m[i + 4], 20, -405537848);
				a = GG(a, b, c, d, m[i + 9], 5, 568446438);
				d = GG(d, a, b, c, m[i + 14], 9, -1019803690);
				c = GG(c, d, a, b, m[i + 3], 14, -187363961);
				b = GG(b, c, d, a, m[i + 8], 20, 1163531501);
				a = GG(a, b, c, d, m[i + 13], 5, -1444681467);
				d = GG(d, a, b, c, m[i + 2], 9, -51403784);
				c = GG(c, d, a, b, m[i + 7], 14, 1735328473);
				b = GG(b, c, d, a, m[i + 12], 20, -1926607734);
				a = HH(a, b, c, d, m[i + 5], 4, -378558);
				d = HH(d, a, b, c, m[i + 8], 11, -2022574463);
				c = HH(c, d, a, b, m[i + 11], 16, 1839030562);
				b = HH(b, c, d, a, m[i + 14], 23, -35309556);
				a = HH(a, b, c, d, m[i + 1], 4, -1530992060);
				d = HH(d, a, b, c, m[i + 4], 11, 1272893353);
				c = HH(c, d, a, b, m[i + 7], 16, -155497632);
				b = HH(b, c, d, a, m[i + 10], 23, -1094730640);
				a = HH(a, b, c, d, m[i + 13], 4, 681279174);
				d = HH(d, a, b, c, m[i + 0], 11, -358537222);
				c = HH(c, d, a, b, m[i + 3], 16, -722521979);
				b = HH(b, c, d, a, m[i + 6], 23, 76029189);
				a = HH(a, b, c, d, m[i + 9], 4, -640364487);
				d = HH(d, a, b, c, m[i + 12], 11, -421815835);
				c = HH(c, d, a, b, m[i + 15], 16, 530742520);
				b = HH(b, c, d, a, m[i + 2], 23, -995338651);
				a = II(a, b, c, d, m[i + 0], 6, -198630844);
				d = II(d, a, b, c, m[i + 7], 10, 1126891415);
				c = II(c, d, a, b, m[i + 14], 15, -1416354905);
				b = II(b, c, d, a, m[i + 5], 21, -57434055);
				a = II(a, b, c, d, m[i + 12], 6, 1700485571);
				d = II(d, a, b, c, m[i + 3], 10, -1894986606);
				c = II(c, d, a, b, m[i + 10], 15, -1051523);
				b = II(b, c, d, a, m[i + 1], 21, -2054922799);
				a = II(a, b, c, d, m[i + 8], 6, 1873313359);
				d = II(d, a, b, c, m[i + 15], 10, -30611744);
				c = II(c, d, a, b, m[i + 6], 15, -1560198380);
				b = II(b, c, d, a, m[i + 13], 21, 1309151649);
				a = II(a, b, c, d, m[i + 4], 6, -145523070);
				d = II(d, a, b, c, m[i + 11], 10, -1120210379);
				c = II(c, d, a, b, m[i + 2], 15, 718787259);
				b = II(b, c, d, a, m[i + 9], 21, -343485551);
				a = a + aa >>> 0;
				b = b + bb >>> 0;
				c = c + cc >>> 0;
				d = d + dd >>> 0;
			}
			return crypt.endian([
				a,
				b,
				c,
				d
			]);
		};
		md5._ff = function(a, b, c, d, x, s, t) {
			var n = a + (b & c | ~b & d) + (x >>> 0) + t;
			return (n << s | n >>> 32 - s) + b;
		};
		md5._gg = function(a, b, c, d, x, s, t) {
			var n = a + (b & d | c & ~d) + (x >>> 0) + t;
			return (n << s | n >>> 32 - s) + b;
		};
		md5._hh = function(a, b, c, d, x, s, t) {
			var n = a + (b ^ c ^ d) + (x >>> 0) + t;
			return (n << s | n >>> 32 - s) + b;
		};
		md5._ii = function(a, b, c, d, x, s, t) {
			var n = a + (c ^ (b | ~d)) + (x >>> 0) + t;
			return (n << s | n >>> 32 - s) + b;
		};
		md5._blocksize = 16;
		md5._digestsize = 16;
		module.exports = function(message, options) {
			if (message === void 0 || message === null) throw new Error("Illegal argument " + message);
			var digestbytes = crypt.wordsToBytes(md5(message, options));
			return options && options.asBytes ? digestbytes : options && options.asString ? bin.bytesToString(digestbytes) : crypt.bytesToHex(digestbytes);
		};
	})();
}));
//#endregion
//#region node_modules/bakong-khqr/src/model/response.js
var require_response = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var md5 = require_md5();
	var KHQRData = class {
		constructor(qr) {
			this.qr = qr;
			this.md5 = md5(qr);
		}
		getData() {
			return {
				qr: this.qr,
				md5: this.md5
			};
		}
	};
	var KHQRDeepLinkData = class {
		constructor(shortLink) {
			this.shortLink = shortLink;
		}
		getData() {
			return { shortLink: this.shortLink };
		}
	};
	/**
	* A function for returning the KHQR response
	* the response has format
	* { status: { code: 0, errorCode: 0, message: '' },
	*   data: null }
	* How to use:
	* when error pass KHQRResponse(null, errorObject)
	* when success pass KHQRResponse(data, null)
	* @param {any} data
	* @param {any} errorObject
	* @returns
	*/
	function KHQRResponse(data, errorObject) {
		const isError = errorObject == null;
		return {
			status: {
				code: !isError ? 1 : 0,
				errorCode: isError ? null : errorObject.code,
				message: isError ? null : errorObject.message
			},
			data
		};
	}
	module.exports = {
		KHQRData,
		KHQRDeepLinkData,
		KHQRResponse
	};
}));
//#endregion
//#region node_modules/bakong-khqr/src/model/index.js
var require_model = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = require_response();
}));
//#endregion
//#region node_modules/bakong-khqr/src/helper/deeplink.js
var require_deeplink = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var axios = require_axios();
	var { errorCode } = require_constant();
	var { KHQRResponse } = require_model();
	var SourceInfo = class {
		constructor(appIconUrl, appName, appDeepLinkCallback) {
			this.appIconUrl = appIconUrl || null;
			this.appName = appName || null;
			this.appDeepLinkCallback = appDeepLinkCallback || null;
		}
	};
	function isValidLink(link) {
		try {
			if (new URL(link).pathname != "/v1/generate_deeplink_by_qr") return false;
		} catch (error) {
			return false;
		}
		return true;
	}
	async function callDeepLinkAPI(url, data) {
		try {
			let response = await axios.post(url, data, {
				headers: { "Content-Type": "application/json" },
				timeout: 45 * 1e3
			});
			const respBody = response.data;
			const error = respBody.errorCode;
			if (response == void 0) throw KHQRResponse(null, errorCode.CONNECTION_TIMEOUT);
			if (error == 5) return KHQRResponse(null, errorCode.INVALID_DEEP_LINK_SOURCE_INFO);
			else if (error == 4) return KHQRResponse(null, errorCode.INTERNAL_SERVER_ERROR);
			return respBody;
		} catch (error) {
			if (error.code === "ECONNABORTED") throw KHQRResponse(null, errorCode.CONNECTION_TIMEOUT);
			throw KHQRResponse(null, errorCode.CONNECTION_TIMEOUT);
		}
	}
	module.exports = {
		isValidLink,
		callDeepLinkAPI,
		SourceInfo
	};
}));
//#endregion
//#region node_modules/bakong-khqr/src/model/information.js
var require_information = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var { khqrData } = require_constant();
	var removeEmptyElement = (obj) => {
		return Object.fromEntries(Object.entries(obj).filter(([_, v]) => v != null).filter(([_, v]) => v != void 0).filter(([_, v]) => v != ""));
	};
	var IndividualInfo = class {
		/**
		* Individual information
		* fill the params down below
		* @param {*} bakongAccountID
		* @param {*} merchantName
		* @param {*} merchantCity
		* @param {*} optional
		*/
		constructor(bakongAccountID, merchantName, merchantCity, optional = {}) {
			optional = removeEmptyElement(optional);
			if (this.isObject(bakongAccountID) || this.isObject(merchantName) || this.isObject(merchantCity)) throw "bakongAccountID, merchantName or merchantCity must be a string";
			this.bakongAccountID = bakongAccountID;
			this.accountInformation = optional.accountInformation;
			this.acquiringBank = optional.acquiringBank;
			this.currency = optional.currency == void 0 ? khqrData.currency.khr : optional.currency;
			this.amount = optional.amount;
			this.merchantName = merchantName;
			this.merchantCity = merchantCity;
			this.billNumber = optional.billNumber;
			this.storeLabel = optional.storeLabel;
			this.terminalLabel = optional.terminalLabel;
			this.mobileNumber = optional.mobileNumber;
			this.purposeOfTransaction = optional.purposeOfTransaction;
			this.languagePreference = optional.languagePreference;
			this.merchantNameAlternateLanguage = optional.merchantNameAlternateLanguage;
			this.merchantCityAlternateLanguage = optional.merchantCityAlternateLanguage;
			this.upiMerchantAccount = optional.upiMerchantAccount;
			this.expirationTimestamp = optional.expirationTimestamp;
			this.merchantCategoryCode = optional.merchantCategoryCode;
		}
		isObject(val) {
			return val instanceof Object;
		}
	};
	var MerchantInfo = class extends IndividualInfo {
		/**
		* Merchant information
		* Fill the params down below
		* @param {*} bakongAccountID
		* @param {*} merchantName
		* @param {*} merchantCity
		* @param {*} merchantID
		* @param {*} acquiringBank
		* @param {*} optional
		*/
		constructor(bakongAccountID, merchantName, merchantCity, merchantID, acquiringBank, optional = {}) {
			optional = removeEmptyElement(optional);
			super(bakongAccountID, merchantName, merchantCity, optional);
			if (this.isObject(merchantID) || this.isObject(acquiringBank)) throw "merchantID and acquiringBank must be a string";
			this.amount = optional.amount;
			this.merchantID = merchantID;
			this.acquiringBank = acquiringBank;
		}
		isObject(val) {
			return val instanceof Object;
		}
	};
	module.exports = {
		IndividualInfo,
		MerchantInfo
	};
}));
//#endregion
//#region node_modules/bakong-khqr/src/tagLengthString.js
var require_tagLengthString = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var TagLengthString = class {
		constructor(tag, value) {
			this.tag = tag;
			this.value = value;
			const length = String(value).length;
			this.length = length < 10 ? `0${length}` : String(length);
		}
		toString() {
			return `${this.tag}${this.length}${this.value}`;
		}
	};
	module.exports = TagLengthString;
}));
//#endregion
//#region node_modules/bakong-khqr/src/MerchantCode/pointOfInitiationMethod.js
var require_pointOfInitiationMethod = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var { emv, errorCode } = require_constant();
	var { KHQRResponse } = require_model();
	var TagLengthString = require_tagLengthString();
	var PointOfInitiationMethod = class extends TagLengthString {
		constructor(tag, value) {
			if (value.length > 2) throw KHQRResponse(null, errorCode.POINT_INITIATION_LENGTH_INVALID);
			if (value !== emv.STATIC_QR && value !== emv.DYNAMIC_QR) throw KHQRResponse(null, errorCode.POINT_OF_INITIATION_METHOD_INVALID);
			super(tag, value);
		}
	};
	module.exports = PointOfInitiationMethod;
}));
//#endregion
//#region node_modules/bakong-khqr/src/MerchantCode/payloadFormatIndicator.js
var require_payloadFormatIndicator = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var { emv, errorCode } = require_constant();
	var { KHQRResponse } = require_model();
	var TagLengthString = require_tagLengthString();
	var PayloadFormatIndicator = class extends TagLengthString {
		constructor(tag, value) {
			if (value == "" || value == null || value == void 0) throw KHQRResponse(null, errorCode.PAYLOAD_FORMAT_INDICATOR_TAG_REQUIRED);
			else if (value.length > 2) throw KHQRResponse(null, errorCode.PAYLOAD_FORMAT_INDICATOR_LENGTH_INVALID);
			super(tag, value);
		}
	};
	module.exports = PayloadFormatIndicator;
}));
//#endregion
//#region node_modules/bakong-khqr/src/MerchantCode/CRC.js
var require_CRC = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var { errorCode } = require_constant();
	var { KHQRResponse } = require_model();
	var TagLengthString = require_tagLengthString();
	var CRC = class extends TagLengthString {
		constructor(tag, value) {
			if (value == "" || value == null || value == void 0) throw KHQRResponse(null, errorCode.CRC_TAG_REQUIRED);
			else if (value.length > 4) throw KHQRResponse(null, errorCode.CRC_LENGTH_INVALID);
			super(tag, value);
		}
	};
	module.exports = CRC;
}));
//#endregion
//#region node_modules/bakong-khqr/src/MerchantCode/globalUniqueIdentifier.js
var require_globalUniqueIdentifier = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var TagLengthString = require_tagLengthString();
	var { emv, errorCode } = require_constant();
	var { KHQRResponse } = require_model();
	var GlobalUniqueIdentifier = class extends TagLengthString {
		constructor(tag, valueObject) {
			if (valueObject == null) throw KHQRResponse(null, errorCode.MERCHANT_TYPE_REQUIRED);
			if (valueObject == null) valueObject = {
				bakongAccountID: null,
				merchantID: null,
				acquiringBank: null,
				accountInformation: null
			};
			const bakongAccountID = valueObject.bakongAccountID;
			const merchantID = valueObject.merchantID;
			const acquiringBank = valueObject.acquiringBank;
			const isMerchant = valueObject.isMerchant;
			const accountInformation = valueObject.accountInformation;
			const bakongAccountId = new BakongAccountID(emv.BAKONG_ACCOUNT_IDENTIFIER, bakongAccountID);
			let globalUniqueIdentifier = bakongAccountId.toString();
			if (isMerchant) {
				const merchantId = new MerchantId(emv.MERCHANT_ACCOUNT_INFORMATION_MERCHANT_ID, merchantID);
				const acquiringBankName = new AcquiringBank(emv.MERCHANT_ACCOUNT_INFORMATION_ACQUIRING_BANK, acquiringBank);
				if (merchantID != void 0) globalUniqueIdentifier += merchantId.toString();
				if (acquiringBank != void 0) globalUniqueIdentifier += acquiringBankName.toString();
				super(tag, globalUniqueIdentifier);
				this.merchantID = merchantId;
				this.acquiringBank = acquiringBankName;
				this.data = {
					bakongAccountID: bakongAccountId,
					merchantID: merchantId,
					acquiringBank: acquiringBankName
				};
			} else {
				if (accountInformation != void 0) {
					const accInformation = new AccountInformation(emv.INDIVIDUAL_ACCOUNT_INFORMATION, accountInformation);
					globalUniqueIdentifier += accInformation.toString();
				}
				if (acquiringBank != void 0) {
					const acquiringBankName = new AcquiringBank(emv.MERCHANT_ACCOUNT_INFORMATION_ACQUIRING_BANK, acquiringBank);
					globalUniqueIdentifier += acquiringBankName.toString();
				}
				super(tag, globalUniqueIdentifier);
				this.accountInformation = accountInformation;
				this.data = {
					bakongAccountID: bakongAccountId,
					accountInformation
				};
			}
			this.bakongAccountID = bakongAccountId;
		}
	};
	var BakongAccountID = class extends TagLengthString {
		constructor(tag, bakongAccountID) {
			if (bakongAccountID == "" || bakongAccountID == null || bakongAccountID == void 0) throw KHQRResponse(null, errorCode.BAKONG_ACCOUNT_ID_REQUIRED);
			const bakongAccountDivide = bakongAccountID.split("@");
			if (bakongAccountID.length > emv.INVALID_LENGTH.BAKONG_ACCOUNT) throw KHQRResponse(null, errorCode.BAKONG_ACCOUNT_ID_LENGTH_INVALID);
			else if (bakongAccountDivide.length < 2) throw KHQRResponse(null, errorCode.BAKONG_ACCOUNT_ID_INVALID);
			super(tag, bakongAccountID);
		}
	};
	var AccountInformation = class extends TagLengthString {
		constructor(tag, value) {
			if (value.length > emv.INVALID_LENGTH.ACCOUNT_INFORMATION) throw KHQRResponse(null, errorCode.ACCOUNT_INFORMATION_LENGTH_INVALID);
			super(tag, value);
		}
	};
	var MerchantId = class extends TagLengthString {
		constructor(tag, value) {
			if (value == "" || value == null || value == void 0) throw KHQRResponse(null, errorCode.MERCHANT_ID_REQUIRED);
			else if (value.length > emv.INVALID_LENGTH.MERCHANT_ID) throw KHQRResponse(null, errorCode.MERCHANT_ID_LENGTH_INVALID);
			super(tag, value);
		}
	};
	var AcquiringBank = class extends TagLengthString {
		constructor(tag, value) {
			if (value == "" || value == null || value == void 0) throw KHQRResponse(null, errorCode.ACQUIRING_BANK_REQUIRED);
			else if (value.length > emv.INVALID_LENGTH.ACQUIRING_BANK) throw KHQRResponse(null, errorCode.ACQUIRING_BANK_LENGTH_INVALID);
			super(tag, value);
		}
	};
	module.exports = GlobalUniqueIdentifier;
}));
//#endregion
//#region node_modules/bakong-khqr/src/MerchantCode/transactionCurrency.js
var require_transactionCurrency = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var { emv, errorCode, khqrData } = require_constant();
	var { KHQRResponse } = require_model();
	var TagLengthString = require_tagLengthString();
	var TransactionCurrency = class extends TagLengthString {
		constructor(tag, value) {
			if (value == "" || value == null || value == void 0) throw KHQRResponse(null, errorCode.CURRENCY_TYPE_REQUIRED);
			else if (value.length > 3) throw KHQRResponse(null, errorCode.TRANSACTION_CURRENCY_LENGTH_INVALID);
			else if (![khqrData.currency.khr, khqrData.currency.usd].includes(parseInt(value))) throw KHQRResponse(null, errorCode.UNSUPPORTED_CURRENCY);
			super(tag, value);
		}
	};
	module.exports = TransactionCurrency;
}));
//#endregion
//#region node_modules/bakong-khqr/src/MerchantCode/countryCode.js
var require_countryCode = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var { emv, errorCode } = require_constant();
	var { KHQRResponse } = require_model();
	var TagLengthString = require_tagLengthString();
	var CountryCode = class extends TagLengthString {
		constructor(tag, value) {
			if (value == "" || value == null || value == void 0) throw KHQRResponse(null, errorCode.COUNTRY_CODE_TAG_REQUIRED);
			else if (value.length > emv.INVALID_LENGTH.COUNTRY_CODE) throw KHQRResponse(null, errorCode.COUNTRY_CODE_LENGTH_INVALID);
			super(tag, value);
		}
	};
	module.exports = CountryCode;
}));
//#endregion
//#region node_modules/bakong-khqr/src/MerchantCode/merchantCity.js
var require_merchantCity = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var { emv, errorCode } = require_constant();
	var { KHQRResponse } = require_model();
	var TagLengthString = require_tagLengthString();
	var MerchantCity = class extends TagLengthString {
		constructor(tag, value) {
			if (value == "" || value == null || value == void 0) throw KHQRResponse(null, errorCode.MERCHANT_CITY_TAG_REQUIRED);
			else if (value.length > emv.INVALID_LENGTH.MERCHANT_CITY) throw KHQRResponse(null, errorCode.MERCHANT_CITY_LENGTH_INVALID);
			super(tag, value);
		}
	};
	module.exports = MerchantCity;
}));
//#endregion
//#region node_modules/bakong-khqr/src/MerchantCode/merchantCategoryCode.js
var require_merchantCategoryCode = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var { emv, errorCode } = require_constant();
	var { KHQRResponse } = require_model();
	var TagLengthString = require_tagLengthString();
	var MerchantCategoryCode = class extends TagLengthString {
		constructor(tag, value) {
			if (value == "" || value == null || value == void 0) throw KHQRResponse(null, errorCode.MERCHANT_CATEGORY_TAG_REQUIRED);
			else if (value.length > emv.INVALID_LENGTH.MERCHANT_CATEGORY_CODE) throw KHQRResponse(null, errorCode.MERCHANT_CODE_LENGTH_INVALID);
			else if (!/^\d+$/.test(value)) throw KHQRResponse(null, errorCode.INVALID_MERCHANT_CATEGORY_CODE);
			else {
				const mcc = parseInt(value, 10);
				if (mcc < 0 || mcc > 9999) throw KHQRResponse(null, errorCode.INVALID_MERCHANT_CATEGORY_CODE);
			}
			super(tag, value);
		}
	};
	module.exports = MerchantCategoryCode;
}));
//#endregion
//#region node_modules/bakong-khqr/src/MerchantCode/transactionAmount.js
var require_transactionAmount = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var { emv, errorCode } = require_constant();
	var { KHQRResponse } = require_model();
	var TagLengthString = require_tagLengthString();
	var TransactionAmount = class extends TagLengthString {
		constructor(tag, value) {
			if (String(value).length > emv.INVALID_LENGTH.AMOUNT || String(value).includes("-") || value == "" || value == void 0 || value == null) throw KHQRResponse(null, errorCode.TRANSACTION_AMOUNT_INVALID);
			super(tag, value);
		}
	};
	module.exports = TransactionAmount;
}));
//#endregion
//#region node_modules/bakong-khqr/src/MerchantCode/merchantName.js
var require_merchantName = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var { emv, errorCode } = require_constant();
	var { KHQRResponse } = require_model();
	var TagLengthString = require_tagLengthString();
	var MerchantName = class extends TagLengthString {
		constructor(tag, value) {
			if (value == "" || value == null || value == void 0) throw KHQRResponse(null, errorCode.MERCHANT_NAME_REQUIRED);
			else if (value.length > emv.INVALID_LENGTH.MERCHANT_NAME) throw KHQRResponse(null, errorCode.MERCHANT_NAME_LENGTH_INVALID);
			super(tag, value);
		}
	};
	module.exports = MerchantName;
}));
//#endregion
//#region node_modules/bakong-khqr/src/MerchantCode/timeStamp.js
var require_timeStamp = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var { errorCode, emv } = require_constant();
	var { KHQRResponse } = require_model();
	var TagLengthString = require_tagLengthString();
	var TimeStamp = class extends TagLengthString {
		constructor(tag, timestamp, poi) {
			const creationTimestamp = Number(timestamp?.creationTimestamp);
			const expirationTimestamp = Number(timestamp?.expirationTimestamp);
			if (poi === emv.DYNAMIC_QR) {
				if (!timestamp || !expirationTimestamp) throw KHQRResponse(null, errorCode.EXPIRATION_TIMESTAMP_REQUIRED);
				if (expirationTimestamp.toString().length !== emv.INVALID_LENGTH.TIMESTAMP) throw KHQRResponse(null, errorCode.EXPIRATION_TIMESTAMP_LENGTH_INVALID);
				if (isNaN(new Date(expirationTimestamp).getTime())) throw KHQRResponse(null, errorCode.INVALID_DYNAMIC_KHQR);
				if (expirationTimestamp < creationTimestamp) throw KHQRResponse(null, errorCode.EXPIRATION_TIMESTAMP_IN_THE_PAST);
				if (expirationTimestamp < Date.now()) throw KHQRResponse(null, errorCode.KHQR_EXPIRED);
			}
			let timestampString = "";
			const createdTimestamp = new TimeStampMiliSecond(emv.CREATION_TIMESTAMP, creationTimestamp);
			timestampString += createdTimestamp.toString();
			const expiredTimestamp = new TimeStampMiliSecond(emv.EXPIRATION_TIMESTAMP, expirationTimestamp);
			timestampString += expiredTimestamp.toString();
			super(tag, timestampString);
		}
	};
	var TimeStampMiliSecond = class extends TagLengthString {
		constructor(tag, value) {
			super(tag, value);
		}
	};
	module.exports = TimeStamp;
}));
//#endregion
//#region node_modules/bakong-khqr/src/MerchantCode/merchantInformationLanguageTemplate.js
var require_merchantInformationLanguageTemplate = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var { emv, errorCode } = require_constant();
	var { KHQRResponse } = require_model();
	var TagLengthString = require_tagLengthString();
	var MerchantInformationLanguageTemplate = class extends TagLengthString {
		constructor(tag, value) {
			if (value == null) value = {
				languagePreference: null,
				merchantNameAlternateLanguage: null,
				merchantCityAlternateLanguage: null
			};
			if (value.languagePreference && !value.merchantNameAlternateLanguage) throw KHQRResponse(null, errorCode.MERCHANT_NAME_ALTERNATE_LANGUAGE_REQUIRED);
			let merchantInformationLanguageTemplateString = "";
			if (value.merchantNameAlternateLanguage != void 0) {
				const perference = new LanguagePreference(emv.LANGUAGE_PREFERENCE, value.languagePreference);
				merchantInformationLanguageTemplateString += perference.toString();
			}
			if (value.merchantNameAlternateLanguage != void 0) {
				const alterName = new MerchantNameAlternateLanguage(emv.MERCHANT_NAME_ALTERNATE_LANGUAGE, value.merchantNameAlternateLanguage);
				merchantInformationLanguageTemplateString += alterName.toString();
			}
			if (value.merchantCityAlternateLanguage != void 0) {
				const alterCity = new MerchantCityAlternateLanguage(emv.MERCHANT_CITY_ALTERNATE_LANGUAGE, value.merchantCityAlternateLanguage);
				merchantInformationLanguageTemplateString += alterCity.toString();
			}
			super(tag, merchantInformationLanguageTemplateString);
			this.data = value;
		}
	};
	var LanguagePreference = class extends TagLengthString {
		constructor(tag, value) {
			if (value.length > emv.INVALID_LENGTH.LANGUAGE_PREFERENCE || value == "") throw KHQRResponse(null, errorCode.LANGUAGE_PREFERENCE_LENGTH_INVALID);
			super(tag, value);
		}
	};
	var MerchantNameAlternateLanguage = class extends TagLengthString {
		constructor(tag, value) {
			if (value.length > emv.INVALID_LENGTH.MERCHANT_NAME_ALTERNATE_LANGUAGE || value == "") throw KHQRResponse(null, errorCode.MERCHANT_NAME_ALTERNATE_LANGUAGE_LENGTH_INVALID);
			super(tag, value);
		}
	};
	var MerchantCityAlternateLanguage = class extends TagLengthString {
		constructor(tag, value) {
			if (value.length > emv.INVALID_LENGTH.MERCHANT_CITY_ALTERNATE_LANGUAGE || value == "") throw KHQRResponse(null, errorCode.MERCHANT_CITY_ALTERNATE_LANGUAGE_LENGTH_INVALID);
			super(tag, value);
		}
	};
	module.exports = MerchantInformationLanguageTemplate;
}));
//#endregion
//#region node_modules/bakong-khqr/src/MerchantCode/unionPayMerchant.js
var require_unionPayMerchant = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var { emv, errorCode } = require_constant();
	var { KHQRResponse } = require_model();
	var TagLengthString = require_tagLengthString();
	var UnionpayMerchantAccount = class extends TagLengthString {
		constructor(tag, value) {
			if (value.length > emv.INVALID_LENGTH.UPI_MERCHANT) throw KHQRResponse(null, errorCode.UPI_ACCOUNT_INFORMATION_LENGTH_INVALID);
			super(tag, value);
		}
	};
	module.exports = UnionpayMerchantAccount;
}));
//#endregion
//#region node_modules/bakong-khqr/src/MerchantCode/additionalData.js
var require_additionalData = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var { emv, errorCode } = require_constant();
	var { KHQRResponse } = require_model();
	var TagLengthString = require_tagLengthString();
	var AdditionalData = class extends TagLengthString {
		constructor(tag, additionalData) {
			if (additionalData == null) additionalData = {
				billNumberInput: null,
				mobileNumberInput: null,
				storeLabelInput: null,
				terminalLabelInput: null
			};
			const billNumberInput = additionalData.billNumber;
			const mobileNumberInput = additionalData.mobileNumber;
			const storeLabelInput = additionalData.storeLabel;
			const terminalLabelInput = additionalData.terminalLabel;
			const purposeOfTransaction = additionalData.purposeOfTransaction;
			let billNumber;
			let mobileNumber;
			let storeLabel;
			let terminalLabel;
			let additionalDataString = "";
			if (billNumberInput != void 0) {
				billNumber = new BillNumber(emv.BILLNUMBER_TAG, billNumberInput);
				additionalDataString += billNumber.toString();
			}
			if (mobileNumberInput != void 0) {
				mobileNumber = new MobileNumber(emv.ADDITIONAL_DATA_FIELD_MOBILE_NUMBER, mobileNumberInput);
				additionalDataString += mobileNumber.toString();
			}
			if (storeLabelInput != void 0) {
				storeLabel = new StoreLabel(emv.STORELABEL_TAG, storeLabelInput);
				additionalDataString += storeLabel.toString();
			}
			if (terminalLabelInput != void 0) {
				terminalLabel = new TerminalLabel(emv.TERMINAL_TAG, terminalLabelInput);
				additionalDataString += terminalLabel.toString();
			}
			if (purposeOfTransaction != void 0) {
				const purpose = new PurposeOfTransaction(emv.PURPOSE_OF_TRANSACTION, purposeOfTransaction);
				additionalDataString += purpose.toString();
			}
			super(tag, additionalDataString);
			this.billNumber = billNumber;
			this.mobileNumber = mobileNumber;
			this.storeLabel = storeLabel;
			this.terminalLabel = terminalLabel;
			this.data = {
				billNumber,
				mobileNumber,
				storeLabel,
				terminalLabel,
				purposeOfTransaction
			};
		}
	};
	var BillNumber = class extends TagLengthString {
		constructor(tag, value) {
			if (value.length > emv.INVALID_LENGTH.BILL_NUMBER || value == "") throw KHQRResponse(null, errorCode.BILL_NUMBER_LENGTH_INVALID);
			super(tag, value);
		}
	};
	var StoreLabel = class extends TagLengthString {
		constructor(tag, value) {
			if (value.length > emv.INVALID_LENGTH.STORE_LABEL || value == "") throw KHQRResponse(null, errorCode.STORE_LABEL_LENGTH_INVALID);
			super(tag, value);
		}
	};
	var TerminalLabel = class extends TagLengthString {
		constructor(tag, value) {
			if (value.length > emv.INVALID_LENGTH.TERMINAL_LABEL || value == "") throw KHQRResponse(null, errorCode.TERMINAL_LABEL_LENGTH_INVALID);
			super(tag, value);
		}
	};
	var MobileNumber = class extends TagLengthString {
		constructor(tag, value) {
			if (value.length > emv.INVALID_LENGTH.MOBILE_NUMBER || value == "") throw KHQRResponse(null, errorCode.MOBILE_NUMBER_LENGTH_INVALID);
			super(tag, value);
		}
	};
	var PurposeOfTransaction = class extends TagLengthString {
		constructor(tag, value) {
			if (value.length > emv.INVALID_LENGTH.PURPOSE_OF_TRANSACTION || value == "") throw KHQRResponse(null, errorCode.PURPOSE_OF_TRANSACTION_LENGTH_INVALID);
			super(tag, value);
		}
	};
	module.exports = AdditionalData;
}));
//#endregion
//#region node_modules/bakong-khqr/src/MerchantCode/index.js
var require_MerchantCode = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = {
		PointOfInitiationMethod: require_pointOfInitiationMethod(),
		PayloadFormatIndicator: require_payloadFormatIndicator(),
		CRC: require_CRC(),
		GlobalUnqiueIdentifier: require_globalUniqueIdentifier(),
		TransactionCurrency: require_transactionCurrency(),
		CountryCode: require_countryCode(),
		MerchantCity: require_merchantCity(),
		MerchantCategoryCode: require_merchantCategoryCode(),
		TransactionAmount: require_transactionAmount(),
		MerchantName: require_merchantName(),
		TimeStamp: require_timeStamp(),
		MerchantInformationLanguageTemplate: require_merchantInformationLanguageTemplate(),
		UnionpayMerchantAccount: require_unionPayMerchant(),
		AdditionalData: require_additionalData()
	};
}));
//#endregion
//#region node_modules/bakong-khqr/src/controller/generateKHQR.js
var require_generateKHQR = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var { emv, khqrData, errorCode } = require_constant();
	var crc16 = require_crc16();
	var { PointOfInitiationMethod, PayloadFormatIndicator, GlobalUnqiueIdentifier, TransactionCurrency, CountryCode, MerchantCity, MerchantCategoryCode, TransactionAmount, MerchantName, TimeStamp, MerchantInformationLanguageTemplate, AdditionalData, UnionpayMerchantAccount } = require_MerchantCode();
	var { KHQRResponse } = require_model();
	/**
	* Generate KHQR helper Function
	* 1. create object of subtags elements
	* 2. create the string by passing tag and value to the instances
	* 3. calculate CRC
	* 4. return the value
	* @param {object} information 
	* @param {string} type 
	* @returns string of KHQR
	*/
	function generateKHQR(information, type) {
		let merchantInfo = {
			bakongAccountID: information.bakongAccountID,
			isMerchant: false
		};
		if (type == "merchant") merchantInfo = {
			bakongAccountID: information.bakongAccountID,
			merchantID: information.merchantID,
			acquiringBank: information.acquiringBank,
			isMerchant: true
		};
		else merchantInfo = {
			bakongAccountID: information.bakongAccountID,
			accountInformation: information.accountInformation,
			acquiringBank: information.acquiringBank,
			isMerchant: false
		};
		const additionalDataInformation = {
			billNumber: information.billNumber,
			mobileNumber: information.mobileNumber,
			storeLabel: information.storeLabel,
			terminalLabel: information.terminalLabel,
			purposeOfTransaction: information.purposeOfTransaction
		};
		const languageInformation = {
			languagePreference: information.languagePreference,
			merchantNameAlternateLanguage: information.merchantNameAlternateLanguage,
			merchantCityAlternateLanguage: information.merchantCityAlternateLanguage
		};
		try {
			const amount = information.amount;
			const payloadFormatIndicator = new PayloadFormatIndicator(emv.PAYLOAD_FORMAT_INDICATOR, emv.DEFAULT_PAYLOAD_FORMAT_INDICATOR);
			let QRType = emv.DYNAMIC_QR;
			if (amount == void 0 || amount == 0) QRType = emv.STATIC_QR;
			const pointOfInitiationMethod = new PointOfInitiationMethod(emv.POINT_OF_INITIATION_METHOD, QRType);
			let upi;
			if (information.upiMerchantAccount) upi = new UnionpayMerchantAccount(emv.UNIONPAY_MERCHANT_ACCOUNT, information.upiMerchantAccount);
			let KHQRType = emv.MERCHANT_ACCOUNT_INFORMATION_INDIVIDUAL;
			if (type == "merchant") KHQRType = emv.MERCHANT_ACCOUNT_INFORMATION_MERCHANT;
			const globalUniqueIdentifier = new GlobalUnqiueIdentifier(KHQRType, merchantInfo);
			const merchantCategoryCode = new MerchantCategoryCode(emv.MERCHANT_CATEGORY_CODE, String(information.merchantCategoryCode ?? emv.DEFAULT_MERCHANT_CATEGORY_CODE));
			const currency = new TransactionCurrency(emv.TRANSACTION_CURRENCY, information.currency);
			if (information.currency == khqrData.currency.usd && upi) throw KHQRResponse(null, errorCode.UPI_ACCOUNT_INFORMATION_INVALID_CURRENCY);
			const KHQRInstances = [
				payloadFormatIndicator,
				pointOfInitiationMethod,
				upi || "",
				globalUniqueIdentifier,
				merchantCategoryCode,
				currency
			];
			if (!(amount == void 0 || amount == 0)) {
				let amountInput = information.amount;
				if (information.currency == khqrData.currency.khr) if (amountInput % 1 == 0) amountInput = Math.round(amountInput);
				else throw KHQRResponse(null, errorCode.TRANSACTION_AMOUNT_INVALID);
				else {
					const precision = String(amountInput).split(".")[1];
					if (precision != void 0 && precision.length > 2) throw KHQRResponse(null, errorCode.TRANSACTION_AMOUNT_INVALID);
					if (precision != void 0) amountInput = parseFloat(amountInput).toFixed(2);
				}
				const amount = new TransactionAmount(emv.TRANSACTION_AMOUNT, amountInput);
				KHQRInstances.push(amount);
			}
			const countryCode = new CountryCode(emv.COUNTRY_CODE, emv.DEFAULT_COUNTRY_CODE);
			KHQRInstances.push(countryCode);
			const merchantName = new MerchantName(emv.MERCHANT_NAME, information.merchantName);
			KHQRInstances.push(merchantName);
			const merchantCity = new MerchantCity(emv.MERCHANT_CITY, String(information.merchantCity || emv.DEFAULT_MERCHANT_CITY));
			KHQRInstances.push(merchantCity);
			let additionalData;
			if (!Object.values(additionalDataInformation).every((el) => el == null || el == void 0 || el == "")) {
				additionalData = new AdditionalData(emv.ADDITIONAL_DATA_TAG, additionalDataInformation);
				KHQRInstances.push(additionalData);
			}
			if (!Object.values(languageInformation).every((el) => el == null || el == void 0 || el == "")) {
				const languageTemplate = new MerchantInformationLanguageTemplate(emv.MERCHANT_INFORMATION_LANGUAGE_TEMPLATE, languageInformation);
				KHQRInstances.push(languageTemplate);
			}
			if (QRType === emv.DYNAMIC_QR) {
				if (!information.expirationTimestamp) throw KHQRResponse(null, errorCode.EXPIRATION_TIMESTAMP_REQUIRED);
				const timeStamp = new TimeStamp(emv.TIMESTAMP_TAG, {
					creationTimestamp: Date.now(),
					expirationTimestamp: information.expirationTimestamp
				}, QRType);
				KHQRInstances.push(timeStamp);
			}
			let khqrNoCrc = "";
			for (let i = 0; i < KHQRInstances.length; i++) khqrNoCrc += KHQRInstances[i].toString();
			let khqr = khqrNoCrc + emv.CRC + emv.CRC_LENGTH;
			khqr += crc16(khqr);
			return khqr;
		} catch (error) {
			return error;
		}
	}
	module.exports = generateKHQR;
}));
//#endregion
//#region node_modules/bakong-khqr/src/constant/KHQRSubtag.js
var require_KHQRSubtag = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var { emv } = require_constant();
	module.exports = {
		input: [
			{
				tag: "29",
				data: {
					bakongAccountID: null,
					accountInformation: null
				}
			},
			{
				tag: "30",
				data: {
					bakongAccountID: null,
					merchantID: null,
					acquiringBank: null
				}
			},
			{
				tag: "62",
				data: {
					billNumber: null,
					mobileNumber: null,
					storeLabel: null,
					terminalLabel: null,
					purposeOfTransaction: null
				}
			},
			{
				tag: "64",
				data: {
					languagePreference: null,
					merchantNameAlternateLanguage: null,
					merchantCityAlternateLanguage: null
				}
			},
			{
				tag: "99",
				data: {
					creationTimestamp: null,
					expirationTimestamp: null
				}
			}
		],
		compare: [
			{
				tag: "29",
				subTag: emv.BAKONG_ACCOUNT_IDENTIFIER,
				name: "bakongAccountID"
			},
			{
				tag: "29",
				subTag: emv.MERCHANT_ACCOUNT_INFORMATION_MERCHANT_ID,
				name: "accountInformation"
			},
			{
				tag: "29",
				subTag: emv.MERCHANT_ACCOUNT_INFORMATION_ACQUIRING_BANK,
				name: "acquiringBank"
			},
			{
				tag: "62",
				subTag: emv.BILLNUMBER_TAG,
				name: "billNumber"
			},
			{
				tag: "62",
				subTag: emv.ADDITIONAL_DATA_FIELD_MOBILE_NUMBER,
				name: "mobileNumber"
			},
			{
				tag: "62",
				subTag: emv.STORELABEL_TAG,
				name: "storeLabel"
			},
			{
				tag: "62",
				subTag: emv.PURPOSE_OF_TRANSACTION,
				name: "purposeOfTransaction"
			},
			{
				tag: "62",
				subTag: emv.TERMINAL_TAG,
				name: "terminalLabel"
			},
			{
				tag: "64",
				subTag: emv.LANGUAGE_PREFERENCE,
				name: "languagePreference"
			},
			{
				tag: "64",
				subTag: emv.MERCHANT_NAME_ALTERNATE_LANGUAGE,
				name: "merchantNameAlternateLanguage"
			},
			{
				tag: "64",
				subTag: emv.MERCHANT_CITY_ALTERNATE_LANGUAGE,
				name: "merchantCityAlternateLanguage"
			},
			{
				tag: "99",
				subTag: emv.CREATION_TIMESTAMP,
				name: "creationTimestamp"
			},
			{
				tag: "99",
				subTag: emv.EXPIRATION_TIMESTAMP,
				name: "expirationTimestamp"
			}
		]
	};
}));
//#endregion
//#region node_modules/bakong-khqr/src/constant/KHQRTag.js
var require_KHQRTag = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var { PointOfInitiationMethod, PayloadFormatIndicator, TransactionCurrency, MerchantCategoryCode, CountryCode, MerchantCity, TransactionAmount, MerchantName, TimeStamp, AdditionalData, CRC, GlobalUnqiueIdentifier, MerchantInformationLanguageTemplate, UnionpayMerchantAccount } = require_MerchantCode();
	module.exports = [
		{
			tag: "00",
			type: "payloadFormatIndicator",
			required: true,
			instance: PayloadFormatIndicator
		},
		{
			tag: "01",
			type: "pointofInitiationMethod",
			required: false,
			instance: PointOfInitiationMethod
		},
		{
			tag: "15",
			type: "unionPayMerchant",
			required: false,
			instance: UnionpayMerchantAccount
		},
		{
			sub: true,
			tag: "29",
			type: "globalUnqiueIdentifier",
			required: true,
			instance: GlobalUnqiueIdentifier
		},
		{
			tag: "52",
			type: "merchantCategoryCode",
			required: true,
			instance: MerchantCategoryCode
		},
		{
			tag: "53",
			type: "transactionCurrency",
			required: true,
			instance: TransactionCurrency
		},
		{
			tag: "54",
			type: "transactionAmount",
			required: false,
			instance: TransactionAmount
		},
		{
			tag: "58",
			type: "countryCode",
			required: true,
			instance: CountryCode
		},
		{
			tag: "59",
			type: "merchantName",
			required: true,
			instance: MerchantName
		},
		{
			tag: "60",
			type: "merchantCity",
			required: true,
			instance: MerchantCity
		},
		{
			tag: "62",
			sub: true,
			type: "additionalData",
			required: false,
			instance: AdditionalData
		},
		{
			tag: "64",
			sub: true,
			type: "merchantInformationLanguageTemplate",
			required: false,
			instance: MerchantInformationLanguageTemplate
		},
		{
			tag: "99",
			sub: true,
			type: "timestamp",
			required: false,
			instance: TimeStamp
		},
		{
			tag: "63",
			type: "crc",
			required: true,
			instance: CRC
		}
	];
}));
//#endregion
//#region node_modules/bakong-khqr/src/helper/cutString.js
var require_cutString = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = function cutString(string) {
		const sliceIndex = 2;
		const tag = string.slice(0, sliceIndex);
		const length = parseInt(string.slice(sliceIndex, sliceIndex * 2));
		return {
			tag,
			value: string.slice(sliceIndex * 2, sliceIndex * 2 + length),
			slicedString: string.slice(sliceIndex * 2 + length)
		};
	};
}));
//#endregion
//#region node_modules/bakong-khqr/src/controller/decodeKHQR.js
var require_decodeKHQR = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var KHQRSubtag = require_KHQRSubtag();
	var KHQRTag = require_KHQRTag();
	var cutString = require_cutString();
	/**
	* Decode helper function
	* This decode funcition has a flow of
	* 1. Slice the string as each KHQR tag and store into memory
	* 2. Check if the required field exist
	* 3. Check if the KHQR Code given is in order or not
	* 4. Get the value of each tag and if there is subtag repeat number 1
	* @param {string} KHQRStringdecode
	*/
	function decodeKHQR(KHQRString) {
		const allField = KHQRTag.map((el) => el.tag);
		const subtag = KHQRTag.filter((el) => el.sub == true).map((obj) => obj.tag);
		let requiredField = KHQRTag.filter((el) => el.required == true).map((el) => el.tag);
		const subTagInput = KHQRSubtag.input;
		const subTagCompare = KHQRSubtag.compare;
		let tags = {};
		let merchantType = null;
		let lastTag = "";
		let isMerchantTag = false;
		while (KHQRString) {
			sliceTagObject = cutString(KHQRString);
			let { tag, value, slicedString } = sliceTagObject;
			if (tag == lastTag) break;
			if (tag == "30") {
				merchantType = "30";
				tag = "29";
				isMerchantTag = true;
			} else if (tag == "29") merchantType = "29";
			if (allField.includes(tag)) {
				tags[tag] = value;
				requiredField = requiredField.filter((el) => el != tag);
			}
			KHQRString = slicedString;
			lastTag = tag;
		}
		let decodeValue = { merchantType };
		subTagInput.map((el) => el.data).forEach((obj) => decodeValue = {
			...decodeValue,
			...obj
		});
		KHQRTag.forEach((khqrTag) => {
			const tag = khqrTag.tag;
			const khqr = KHQRTag.find((el) => el.tag == tag);
			let value = tags[tag] == void 0 ? null : tags[tag];
			let inputValue = value;
			if (subtag.includes(tag)) {
				const inputdata = cloneObject(findTag(subTagInput, tag).data);
				while (value) {
					cutsubstring = cutString(value);
					const subtag = cutsubstring.tag;
					const subtagValue = cutsubstring.value;
					const slicedsubtag = cutsubstring.slicedString;
					let nameSubtag = subTagCompare.filter((el) => el.tag == tag).find((el) => el.subTag == subtag);
					if (nameSubtag != void 0) {
						nameSubtag = nameSubtag.name;
						if (isMerchantTag && nameSubtag == "accountInformation") nameSubtag = "merchantID";
						inputdata[nameSubtag] = subtagValue;
						inputValue = inputdata;
					}
					value = slicedsubtag;
				}
				decodeValue = {
					...decodeValue,
					...inputValue
				};
				try {
					new khqr.instance(tag, inputValue);
				} catch (error) {}
			} else decodeValue[khqr.type] = value;
		});
		return decodeValue;
	}
	/**
	* Helper function for decode
	* It query from the object where tag is the same
	*/
	var findTag = (object, tag) => object.find((el) => el.tag == tag);
	var cloneObject = (obj) => JSON.parse(JSON.stringify(obj));
	module.exports = decodeKHQR;
}));
//#endregion
//#region node_modules/bakong-khqr/src/controller/decodeNonKHQR.js
var require_decodeNonKHQR = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	function isNumberic(value) {
		return /^\d+$/.test(value);
	}
	function isValid(tag, length, value) {
		return isNumberic(tag) && length === value.length;
	}
	function extractTLV(string) {
		const sliceIndex = 2;
		const tag = string.slice(0, sliceIndex);
		const length = parseInt(string.slice(sliceIndex, sliceIndex * 2));
		return {
			tag,
			length,
			value: string.slice(sliceIndex * 2, sliceIndex * 2 + length),
			remainString: string.slice(sliceIndex * 2 + length)
		};
	}
	/**
	* Decode a non-KHQR string
	*
	* @param {String} qr - The QR code string to decode.
	* @returns {Object} The decoded object.
	*/
	function decodeNonKhqr(qr) {
		const firstLevelData = {};
		const finalData = {};
		let remaningQR = qr;
		do {
			const { tag, length, value, remainString } = extractTLV(remaningQR);
			if (!isValid(tag, length, value)) break;
			firstLevelData[tag] = value;
			remaningQR = remainString;
		} while (remaningQR);
		for (const tag in firstLevelData) {
			let remainingValue = firstLevelData[tag];
			finalData[tag] = remainingValue;
			const secondLevelData = {};
			const thirdLevelData = {};
			if (!(tag >= 26 && tag <= 51) && !(tag >= 80 && tag <= 99) && tag !== "64" && tag !== "62") continue;
			if (remainingValue.length >= 6) {
				do {
					const { tag: subTag, length, value: subValue, remainString } = extractTLV(remainingValue);
					if (!isValid(subTag, length, subValue)) break;
					remainingValue = remainString;
					if (tag === "62" && subTag >= 50 && subTag <= 99) {
						let remainingValueL3 = subValue;
						do {
							const { tag: subTagL3, length, value: valueL3, remainString } = extractTLV(remainingValueL3);
							if (!isValid(subTagL3, length, valueL3)) break;
							thirdLevelData[subTagL3] = valueL3;
							remainingValueL3 = remainString;
						} while (remainingValueL3);
					}
					if (Object.keys(thirdLevelData).length > 0) secondLevelData[subTag] = thirdLevelData;
					else secondLevelData[subTag] = subValue;
				} while (remainingValue);
				if (Object.keys(secondLevelData).length > 0) finalData[tag] = secondLevelData;
			}
		}
		return finalData;
	}
	module.exports = decodeNonKhqr;
}));
//#endregion
//#region node_modules/bakong-khqr/src/controller/decodeValidation.js
var require_decodeValidation = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var { errorCode, emv } = require_constant();
	var KHQRSubtag = require_KHQRSubtag();
	var KHQRTag = require_KHQRTag();
	var cutString = require_cutString();
	var { KHQRResponse } = require_model();
	/**
	* Decode helper function
	* This decode funcition has a flow of
	* 1. Slice the string as each KHQR tag and store into memory
	* 2. Check if the required field exist
	* 3. Check if the KHQR Code given is in order or not
	* 4. Get the value of each tag and if there is subtag repeat number 1
	* @param {string} KHQRString
	* @returns object of KHQR decode
	*/
	function decodeKHQRValidation(KHQRString) {
		const allField = KHQRTag.map((el) => el.tag);
		const subtag = KHQRTag.filter((el) => el.sub == true).map((obj) => obj.tag);
		let requiredField = KHQRTag.filter((el) => el.required == true).map((el) => el.tag);
		const subTagInput = KHQRSubtag.input;
		const subTagCompare = KHQRSubtag.compare;
		let tags = [];
		let merchantType = "individual";
		let lastTag = "";
		while (KHQRString) {
			sliceTagObject = cutString(KHQRString);
			let { tag, value, slicedString } = sliceTagObject;
			if (tag == lastTag) break;
			if (tag == "30") {
				merchantType = "merchant";
				tag = "29";
			}
			if (allField.includes(tag)) {
				tags.push({
					tag,
					value
				});
				requiredField = requiredField.filter((el) => el != tag);
			}
			KHQRString = slicedString;
			lastTag = tag;
		}
		if (tags.some((item) => item.tag === "01" && item.value === "12")) {
			if (!tags.some((item) => item.tag === "54")) throw KHQRResponse(null, errorCode.INVALID_DYNAMIC_KHQR);
			if (!tags.some((item) => item.tag === "99")) throw KHQRResponse(null, errorCode.EXPIRATION_TIMESTAMP_REQUIRED);
		}
		if (tags.some((item) => item.tag === "54") && !tags.some((item) => item.tag === "99")) throw KHQRResponse(null, errorCode.EXPIRATION_TIMESTAMP_REQUIRED);
		if (requiredField.length != 0) {
			const requiredTag = requiredField[0];
			const missingInstance = findTag(KHQRTag, requiredTag).instance;
			new missingInstance(requiredTag, null);
		}
		let decodeValue = { merchantType };
		subTagInput.map((el) => el.data).forEach((obj) => decodeValue = {
			...decodeValue,
			...obj
		});
		let poi = null;
		tags.forEach((khqrTag) => {
			const tag = khqrTag.tag;
			const khqr = KHQRTag.find((el) => el.tag == tag);
			let value = khqrTag.value;
			let inputValue = value;
			if (tag === emv.POINT_OF_INITIATION_METHOD) poi = value;
			if (subtag.includes(tag)) {
				const inputdata = cloneObject(findTag(subTagInput, tag).data);
				while (value) {
					cutsubstring = cutString(value);
					const subtag = cutsubstring.tag;
					const subtagValue = cutsubstring.value;
					const slicedsubtag = cutsubstring.slicedString;
					let nameSubtag = subTagCompare.filter((el) => el.tag == tag).find((el) => el.subTag == subtag);
					if (nameSubtag != void 0) {
						nameSubtag = nameSubtag.name;
						inputdata[nameSubtag] = subtagValue;
						inputValue = inputdata;
					}
					value = slicedsubtag;
				}
				decodeValue = {
					...decodeValue,
					...inputValue
				};
				if (tag === emv.TIMESTAMP_TAG) new khqr.instance(tag, inputValue, poi);
				else new khqr.instance(tag, inputValue);
			} else {
				const instance = new khqr.instance(tag, inputValue);
				decodeValue[khqr.type] = instance.value;
			}
		});
		return decodeValue;
	}
	/**
	* Helper function for decode
	* It query from the object where tag is the same
	*/
	var findTag = (object, tag) => object.find((el) => el.tag == tag);
	var cloneObject = (obj) => JSON.parse(JSON.stringify(obj));
	module.exports = decodeKHQRValidation;
}));
//#endregion
//#region node_modules/bakong-khqr/src/helper/checkAccountID.js
var require_checkAccountID = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var { KHQRResponse } = require_model();
	var { errorCode, emv } = require_constant();
	var axios = require_axios();
	async function isAccountIDExist(url, accountID) {
		if (accountID.length > emv.INVALID_LENGTH.BAKONG_ACCOUNT) throw KHQRResponse(null, errorCode.BAKONG_ACCOUNT_ID_LENGTH_INVALID);
		if (accountID.split("@").length != 2) throw KHQRResponse(null, errorCode.BAKONG_ACCOUNT_ID_INVALID);
		try {
			const respData = (await axios.post(url, { accountId: accountID }, {
				header: { "Content-Type": "application/json" },
				timeout: 45 * 1e3
			})).data;
			const responseCode = respData.responseCode;
			const error = respData.errorCode;
			if (error == 11) return { bakongAccountExisted: false };
			if (error == 12) throw KHQRResponse(null, errorCode.BAKONG_ACCOUNT_ID_INVALID);
			if (responseCode == 0) return { bakongAccountExisted: true };
			else return { bakongAccountExisted: false };
		} catch (error) {
			if (error.code === "ECONNABORTED") throw KHQRResponse(null, errorCode.CONNECTION_TIMEOUT);
			throw KHQRResponse(null, errorCode.CONNECTION_TIMEOUT);
		}
	}
	module.exports = isAccountIDExist;
}));
//#endregion
//#region node_modules/bakong-khqr/src/index.js
var require_src = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var { khqrData, errorCode, emv } = require_constant();
	var crc16 = require_crc16();
	var CRCValidation = require_CRCValidation();
	var { isValidLink, callDeepLinkAPI, SourceInfo } = require_deeplink();
	var { KHQRDeepLinkData, KHQRResponse } = require_model();
	var { IndividualInfo, MerchantInfo } = require_information();
	var generateKHQR = require_generateKHQR();
	var decodeKHQRString = require_decodeKHQR();
	var decodeNonKHQR = require_decodeNonKHQR();
	var md5 = require_md5();
	var decodeKHQRValidation = require_decodeValidation();
	var isAccountIDExist = require_checkAccountID();
	var BakongKHQR = class {
		/**
		* Generate Individual
		* Passing indivualInfo which is the instance of the IndividualInfo
		* Example: const indivualInfo = new IndividualInfo("devit@abaa",
		khqrData.currency.khr,
		"devit",
		"Battambang",
		optionalData)
		* @param {*} individualInfo 
		* @returns object of KHQR Response
		*/
		generateIndividual(individualInfo) {
			const khqr = generateKHQR(individualInfo, khqrData.merchantType.individual);
			if (khqr.status != void 0) return khqr;
			return KHQRResponse({
				qr: khqr,
				md5: md5(khqr)
			}, null);
		}
		/**
		* Generate Individual
		* Passing indivualInfo which is the instance of the IndividualInfo
		* Example: const indivualInfo = new MerchantInfo("devit@abaa",
		khqrData.currency.usd,
		"devit",
		"Battambang",
		optionalData)
		* @param {*} individualInfo 
		* @returns String of KHQR
		*/
		generateMerchant(merchantInfo) {
			const khqr = generateKHQR(merchantInfo, khqrData.merchantType.merchant);
			if (khqr.status != void 0) return khqr;
			return KHQRResponse({
				qr: khqr,
				md5: md5(khqr)
			}, null);
		}
		/**
		* Decode function
		* It is a static function
		* so you can use it as const decodeResult = BakongKHQR.decode(KHQRString)
		* @param {String} KHQRString
		* @returns object of KHQR Response
		*/
		static decode(KHQRString) {
			return KHQRResponse(decodeKHQRString(KHQRString), null);
		}
		/**
		* Decode a non-KHQR string
		* 
		* @param {*} KHQRString - The QR code string to decode.
		* @returns {Object} The decoded object.
		*/
		static decodeNonKhqr(KHQRString) {
			return KHQRResponse(decodeNonKHQR(KHQRString), null);
		}
		/**
		* Verify KHQR
		* This function is a static function
		* it can be used as const isVerifiedKHQR = BakongKHQR.verify(KHQRString)
		* @param {String} KHQRString 
		* @returns Verification object
		*/
		static verify(KHQRString) {
			if (!checkCRCRegExp(KHQRString)) return new CRCValidation(false);
			const crc = KHQRString.slice(-4);
			const isValidCRC = new CRCValidation(crc16(KHQRString.slice(0, -4)) == crc.toUpperCase());
			try {
				if (!isValidCRC.isValid || KHQRString.length < emv.INVALID_LENGTH.KHQR) throw KHQRResponse(null, errorCode.KHQR_INVALID);
				decodeKHQRValidation(KHQRString);
				return new CRCValidation(true);
			} catch (error) {
				return new CRCValidation(false);
			}
		}
		/**
		* Generate Deep Link
		* @param {String} url 
		* @param {String} qr 
		* @param {SourceInfo} sourceInfo (optional)
		* @returns 
		*/
		static async generateDeepLink(url, qr, sourceInfo) {
			if (!isValidLink(url)) return KHQRResponse(null, errorCode.INVALID_DEEP_LINK_URL);
			if (!this.verify(qr).isValid) return KHQRResponse(null, errorCode.KHQR_INVALID);
			if (sourceInfo) {
				if (!sourceInfo.appIconUrl || !sourceInfo.appName || !sourceInfo.appDeepLinkCallback) return KHQRResponse(null, errorCode.INVALID_DEEP_LINK_SOURCE_INFO);
			}
			try {
				return KHQRResponse(new KHQRDeepLinkData((await callDeepLinkAPI(url, { qr })).data.shortLink).getData(), null);
			} catch (error) {
				return error;
			}
		}
		/**
		* Check Bakong Account wether it is exist or not
		* @param {*} url 
		* @param {*} bakongID 
		* @returns 
		*/
		static async checkBakongAccount(url, bakongID) {
			try {
				return KHQRResponse(await isAccountIDExist(url, bakongID), null);
			} catch (error) {
				return error;
			}
		}
	};
	function checkCRCRegExp(crc) {
		return /6304[A-Fa-f0-9]{4}$/.test(crc);
	}
	module.exports = {
		BakongKHQR,
		khqrData,
		SourceInfo,
		IndividualInfo,
		MerchantInfo
	};
}));
//#endregion
export default require_src();

//# sourceMappingURL=bakong-khqr.js.map